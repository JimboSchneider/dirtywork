# Issues #147, #148, #149: one listing script — run ledger

Plan: [one listing script plan](../plans/2026-09-08-issues-147-148-149-one-listing-script.md). Base: `ea6dfd5` (main, right after PR #154). Branch: `dirtywork/issues-147-148-149-replace-0908152948-48e910ea`.

## Worker setup

PyPI release checked on 2026-09-08: `dirtywork==0.13.1`, invoked through `pipx run --spec`. Worker: `qwen/qwen3-coder-next` via LM Studio (`--provider openai --base-url http://localhost:1234/v1`), context window 65,536. Image: `dirtywork-worker-pytest:0.13`, network disabled. Verify gate: the default suite with two fix rounds; `--max-turns 60 --timeout 1800`.

Brief shape: fix-first, eight exact `edit_file` pairs with line numbers, no appended block. The brief was generated by a script from the same old/new strings the dry run applied, so nothing was retyped between the dry run and the worker. 192 lines: the longest brief of the day, because two of the pairs replace whole tests and one replaces the 39-line `list_dir` body.

## Result

| Run suffix | Status | Turns | Wall seconds | Prompt tokens | Completion tokens | Completion tokens / wall second | Nudges | Verdict |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `0908152948-48e910ea` | completed | 22 | 130.8 | 302,267 | 4,274 | 32.68 | 0 | accept |

Wall and usage come from the transcript's `run_end`. The rate divides completion tokens by whole-run wall time; it is not model decode throughput.

Tool calls: `read_file` 10, `grep` 1, `edit_file` 8, `bash` 2; `write_file` 0; tool errors 0; truncations 0; trimmed turns 0. Eight edits for eight pairs, every one accepted on the first try; the first edit landed at turn 8 after seven reads of the quoted ranges. Line numbers again held the read count down (the issue #146 run spent 17 turns hunting anchors); the reads that remain are the worker confirming each range before editing it, which is what we want.

`files_changed` = `dirtywork/sandbox/docker.py`, `tests/test_docker_sandbox.py` only. Verify gate (in-container default suite) exit 0 on the first round. Diff byte-identical to the orchestrator's dry-run patch. Net: 49 insertions, 79 deletions.

Sampler (`tools/soak_sampler.sh`): 26 samples in the run window, free memory **2.06–5.21 GiB**, 4 models loaded. That is the lowest of the day (issue #146 run: 5–9 GiB; issue #150 run: 6–10 GiB) with the same resident set; wall time did not suffer (130.8 s for 22 turns), but the host is close to swapping with `qwen3.6-35b-a3b` resident alongside the two workers.

## Host validation

- `tests/test_docker_sandbox.py`: **178 passed** (178 at baseline: four tests removed, four added).
- Full suite, `PYTHONPATH=. pipx run --spec pytest pytest -q -p no:cacheprovider`: **1,703 passed / 9 skipped / 38 deselected**, 99.3 s. Baseline `ea6dfd5`: 1,703 passed.
- Red check (scratch clone, test edits only, baseline production module): 11 failed / 167 passed.
- `git diff --check`: clean.
- The `LIST_SCRIPT` constant imported from the worktree's module was executed in `dirtywork-worker-pytest:0.13` (dash) and `alpine:3.20` (BusyBox) against a directory with a dotfile, a dot-directory, a file named `-`, a name with a newline, a name with a tab, an empty file, a file named `total`, a link to a directory, a link to a file, a dangling link and a self-loop link. Output byte-identical between the two images: link-to-dir and self-loop as `d`, link-to-file with the target's size, dangling as `l`, the newline and tab preserved inside their records.
- `find -L`, the obvious fix for issue #148, was rejected before briefing: on `ln -s . self` it prints a loop warning, drops the entry and exits 1, which `_list_exec` would report as a listing error.

## Preserved receipts

Slug `issues-147-148-149-replace-0908152948-48e910ea`; local receipts at `~/.dirtywork/runs/<slug>/`: `run.json` (with the accept verdict), `transcript.jsonl`, `diff.patch`, and `orchestrator/` with the exact brief, launcher, stdout JSON, stderr, sampler CSV, and the dry-run and worker diffs. Local provenance, not published artifacts.
