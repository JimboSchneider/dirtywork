# SP2.5 SDD ledger — 2026-08-17 (harness robustness)

Verbatim controller ledger from the SDD workspace (rulings inline). Final review: Opus, 'Ready to merge with fixes' → one fix wave → clean.

Spec: docs/superpowers/specs/2026-08-17-sp2.5-harness-robustness-design.md (a43833f, +deltas). Plan e6ef36a.
Branch: sp2.5-harness (worktree .worktrees/sp2.5, from main e6ef36a). Main checkout stays on main = the dirtywork launcher (~/.local/bin/dirtywork self-locating). Workers: `run-task.sh <label> <brief>` → dirtywork run --sandbox none --branch-from sp2.5-harness (qwen/qwen3-coder-next, 60 turns); after a run the worker branch dirtywork/<slug> is ff-merged into sp2.5-harness in .worktrees/sp2.5, then the worker worktree/branch removed. Report = <label>-run.json final_message (+ transcript). Reviews: Agent(sonnet) with reviewer-template.md + constraints.md; final review Opus.

## Pre-flight scan (2026-08-17 11:5x)
| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1↔T2 runner.py | T1 uses int `failures`; T2 replaces with FailureTracker and rewrites the abort sites | consistent (T2 text says replace) |
| T2↔T3 | T3 calls failures.record("empty_reply") | provided by FAILURE_KINDS ✓ |
| T3↔T4 | T4 replaces T3's `continue` with check_progress; T4 needs `args=None` init beside `abort_reason=None` (T2 placement) | consistent |
| T1↔T4 | T4 inserts check_progress after `pending_finish` return | ✓ |
| T4/T5↔T6 | T6 imports DEFAULT_STALL_TURNS, resolve_context_window; passes stall_turns/context_window to Runner | ✓ |
| T6↔T7↔T9 | T6 _build_sandbox calls start() w/o kwargs; T7 adds kwargs with defaults; T9 passes them + sweeps 5 fake start() in test_main | ✓ ordered |
| T6 _execute | run_info "branch_from": args.branch_from — resume args has no branch_from → must use ctx.branch_from | DEFECT → Ruling below |
| T7↔T8/T9 | init_worker_git(branch=) used by docker.py/export.py; resume passes branch=prior branch | ✓ |
| T8↔T9 | load_prior_run keys; _load_resume_target sets args.sandbox/model/image | ✓ (resume parser has no --sandbox; attribute set unconditionally) |
| T9 tests | _ScriptedClient repeats last response; first run --max-turns 1 → max_turns rc 1 | ✓ |
| T10 | guardrails: `rm README.md`, `git symbolic-ref HEAD` in docker mode — believed allowed; live run verifies | watch |
| T2 test wrong_type | read_file(path=5) may not raise TypeError | Ruling: use `_call("t","read_file",{})` (missing required arg → TypeError) |
| each task self-consistency | tests vs code names checked (FailureTracker/ProgressTracker/classify/NUDGES/STALL_NUDGE/resolve_context_window/RunContext/resume.py names) | ✓ |

Ruling: T6 `_execute` run_info uses `ctx.branch_from` (RunContext.branch_from; None on resume) — why: `args.branch_from` does not exist for `resume` — cost if wrong: none.
Ruling: T2 bad_args test uses `_call("t", "read_file", {})` — why: guaranteed TypeError (missing required positional) — cost: none.
Ruling: worker report = run.json final_message + transcript (dirtywork workers cannot write files outside their worktree without committing them) — same as SP2.

## Task log
- Task 1: dispatched (BASE e6ef36a) 11:55
- Task 1 run: model_error after 34 turns (fake `finish` ×4 — the very failure this plan fixes; 489 tests green, uncommitted). Controller committed the worker's tracked changes verbatim as ae40b76 and ff'd sp2.5-harness. Ruling: HOME=worktree in host mode leaves Library/, .rustup/ etc. in the worktree (git add -A would commit 23 MB of caches) → added those dirs to .git/info/exclude (local only) — why: unblock; cost: none. FOLLOW-UP (harness): host mode should point HOME at a scratch dir outside the worktree.
- Task 1 review (Sonnet): spec ✅, quality Approved, 0 Important. Minor (deferred): finish then ≥3 failing calls in the same turn → model_error wins over pending_finish (plan-mandated order); closure `finish()` vs tool "finish" name overlap; "finish" literal in tools.py schema and runner.FINISH_TOOL not tied by one constant; report provenance is the worker's self-quote (reviewer re-ran the 5 task tests: pass).
- Task 1: complete (commits e6ef36a..ae40b76, review clean)
- Task 2: dispatched (BASE ae40b76)
- Task 2 review (Sonnet): spec ✅ except import organisation; 1 Important (4× local `from dirtywork.runner import FailureTracker` instead of the module block), 1 Minor (unused FAILURE_KINDS/FINISH_TOOL imports). Fix round 1 dispatched.
- Task 2: fix round 1/5 (worker max_turns at 30 while re-verifying; diff correct; ctrl committed ca64619) — re-review (Haiku) dispatched; Task 3 dispatched in parallel from ca64619 (BASE ca64619)
- Task 2: fix round 1/5 (2 addressed, 0 open — local imports hoisted, unused imports dropped; commits 72d3200..ca64619)
- Task 2: complete (commits ae40b76..ca64619, review clean after 1 fix round)
- Task 3 run #1: false `completed` at turn 11 (harness item §1.2 in the wild): qwen's edit_file args containing literal think/tool-call tags were parsed by LM Studio's Qwen3-coder chat template as tool-call XML → the call arrived as text and the (old) runner took it as completion. Worker branch discarded (3 lines).
- Ruling: Task 3 brief rewritten — every think/tool-call tag in runner.py and tests is built by concatenation (`_tag()`, `_THINK_OPEN/_CLOSE`, `_TEXT_TOOL_MARKERS = tuple("<" + m for m in (...))`) with a comment explaining why; semantics unchanged — why: the worker's own tool channel cannot carry those literals — cost if wrong: slightly odder source; none functionally. HARNESS LESSON: briefs must never require a local model to write its own template's control tags literally.
- Task 3: re-dispatched (run #2, BASE ca64619)
- TODO at finish: sync the plan file's Task 3 code with the concatenated-tag ruling (docs commit on the branch).
- Task 3 review (Sonnet): spec ✅, Approved, 0 Important. Minor (deferred): `answer` path returns raw content as final_message (think blocks not stripped; spec says content — final review may prefer stripped); _looks_like_tool_json O(n²) worst case on pathological `{` text (bounded by model output).
- Task 3: complete (commits ca64619..ce83c36, review clean, run #2)
- Task 4: dispatched (BASE ce83c36)
- Task 4 run: max_turns 60 (work complete + 524 green; ctrl committed ec75638). Review (Sonnet) dispatched; Task 5 dispatched in parallel (BASE ec75638)
- Task 4 review (Sonnet): spec ❌ + 1 Critical: unrequested check_progress() inside the pending_finish branch → finish after stall_turns-1 idle turns reported as stalled (reviewer reproduced). Fix round 1 dispatched (from 4f4bd63, after Task 5 landed).
- Task 4: fix round 1/5 (worker: fix correct, ended by fake finish; ctrl committed 28323f3) — re-review (Haiku) dispatched. Task 6 dispatched in parallel (BASE 28323f3); Task 5 review (Sonnet) in flight.
- Task 4: fix round 1/5 (1 addressed, 0 open — stall check removed from the finish path + regression test; commits 4f4bd63..28323f3)
- Task 4: complete (commits ce83c36..ec75638 + 28323f3, review clean after 1 fix round)
- Task 5 review (Sonnet): spec ✅, Approved. ⚠️ CLI wiring is Task 6 (confirmed by plan). Minor (deferred): Runner uses truthiness for context_window (0 → table default; plan-mandated, unreachable via resolve_context_window).
- Task 5: complete (commits ec75638..4f4bd63, review clean)
- Task 6 run: max_turns 80; refactor complete + 537 green (all pre-existing tests pass) but the task's 4 new tests + helpers were never added; ctrl committed the refactor (see land.sh output). Fix round 1 (add tests) dispatched before the task review.
- Task 7 dispatched in parallel with Task 6 fix (BASE 5128c88; disjoint files; rebase before ff if needed)
- Task 6 fix round 1: tests added (541 green), landed bfe9974; full task review (Sonnet) dispatched over 28323f3..bfe9974
- Task 7 run: max_turns 70, work complete (542 green), ctrl committed + rebased → 78748d1; review (Sonnet) dispatched. Task 8 dispatched in parallel (BASE 78748d1).
- Task 6 review (Sonnet, range 28323f3..bfe9974): spec ❌, 2 Important (_build_sandbox still reads args.sandbox; duplicated RunDirError handler in main() with ensure_runs_dir moved out of _execute), 1 Minor (two runner import lines). Fix round 2 dispatched.
- Task 7 review (Sonnet): spec ✅, Approved, 0 findings. Task 7: complete (commits bfe9974..78748d1, review clean)
- Task 6: fix round 2/5 (3 addressed, 0 open; commits 78748d1..b7c44af). Controller found (post-review) a stale duplicate `def _parse_args` left at ~line 441 (dead code; last definition wins). Ruling: fold its deletion into Task 9 (which rewrites _parse_args) — why: same function, one dispatch — cost: none.
- Task 6: complete (commits 28323f3..bfe9974 + b7c44af, review clean after 2 fix rounds; stale _parse_args carried to Task 9)
- Task 8 landed 06d7fad (rebased); review (Sonnet) in flight. Task 9 dispatched (BASE 06d7fad).
- Task 8 review (Sonnet): spec ✅, Approved. Minor (deferred): commit_exists placed at end of workspace.py (not beside worktree_base_commit); check_resumable happy path untested; corrupt run.json path untested; extra-key passthrough only incidental.
- Task 8: complete (commits b7c44af..06d7fad, review clean)
- Task 9 landed 4bc2a67 (worker committed, then max_turns 80); review (Sonnet) in flight. Task 10 dispatched (BASE 4bc2a67); ruling: worker writes the live test + runs unit suite only; controller runs -m docker.
- Task 9 review (Sonnet): spec ✅ (all 4 named risks verified), Approved but 1 Important (plan-mandated: docker fake grep/bash lack Protocol's timeout kwarg → Ruling: fix to match Protocol) + 2 Minor (function-local stdlib imports; trailing whitespace) → fix round 1 dispatched (batched). Task 10 landed 0ef457b; controller ran live: resume test PASS (4.3s); full docker-live suite 12 passed (62s). Task 10 review + Task 11 dispatch next.
- Task 11 dispatched in parallel with Task 9 fix (BASE 0ef457b; README only)
- Task 9: fix round 1/5 (Important + whitespace + json/pytest/os/shutil imports addressed; 6 local `from pathlib import Path` left; landed 1f05b52) → fix round 2 dispatched. Task 10 review (Sonnet): spec ✅ Approved, Minor (deferred): 2 local imports in the new live test. Task 10: complete (commits 4bc2a67..0ef457b, review clean; live suite 12 passed).
- Task 9 fix round 2 run #1: worker stalled at "No module named pytest" (20 turns, nothing done). Root cause: host mode sets HOME=worktree (documented, intended) so the operator's user-site pytest (~/Library/Python/3.9) is invisible; earlier workers silently ran `pip install pytest` into the worktree HOME each run (the Library/ 23 MB seen at Task 1). Ruling: preamble pins `PYTHONUSERBASE=/Users/jimschneider/Library/Python/3.9 python3 -m pytest -q` and forbids pip install — why: unblock + stop per-run installs — cost: none. FOLLOW-UP (harness/docs): host mode should surface this (or add site.getuserbase() to PYTHONUSERBASE) — the same lesson class as HOME=worktree caches.
- Task 9: fix round 2/5 landed faf7aec (6 local Path imports removed). Re-review (Haiku) over 0ef457b..faf7aec dispatched.
- Task 9: fix rounds 1-2/5 (3 addressed, 0 open; commits 0ef457b..faf7aec, test_main.py only)
- Task 9: complete (commits 06d7fad..4bc2a67 + 1f05b52 + faf7aec, review clean after 2 fix rounds)
- Plan file synced with the Task 3 concatenated-tag ruling: 640f0a5 (docs commit on the branch).
- Task 11 review (Sonnet): spec ❌, 2 Important (finish/run-finished note replaced by a duplicated sentence; 'base_commit only' sentence now false), 3 Minor (status order, fence style, resumed_by wording). Fix round 1 dispatched (all five batched).
- Task 11: fix round 1/5 (4 addressed, 1 open — resume block fence opener merged with its first line; commits 640f0a5..287c692). Fix round 2 dispatched.
- Task 11: fix round 2/5 (1 addressed, 0 open; commits 287c692..f1ee6ab)
- Task 11: complete (commits 1f05b52..603f8cb + 287c692 + f1ee6ab, review clean after 2 fix rounds)
- ALL 11 TASKS COMPLETE. Branch head f1ee6ab; unit suite 564 passed; docker-live 12 passed (controller ran both at 287c692; f1ee6ab is README-only after that). Final whole-branch review (Opus) dispatched over e6ef36a..f1ee6ab.
- Smoke (controller, real qwen, branch code, host mode): run → completed in 3 turns (plain-answer completion; run.json has task/model/context_window=65536); `dirtywork resume <slug>` → completed in 1 turn, resumed_from set, prior run.json resumed_by set.
- FINAL REVIEW (Opus): Ready to merge WITH FIXES. 3 Important: (1) seeded finalize deletes the prior host worktree before an export that can fail → Ruling: stash aside, remove after ok / restore after failure; (2) two consecutive user messages when a class nudge or malformed-calls message coincides with the stall nudge → Ruling: merge into one user message per turn (check_progress returns text); (3) check_resumable accepts any dir with a `.git` entry → Ruling: require a linked worktree of the recorded repo (`.git` file whose gitdir resolves under repo/.git). Minors folded into the same wave: chained-resume preamble stacking (RESUME_MARKER split), seed timeout T_EXPORT_STEP + named timeout, context_window `is not None`, first-threshold abort reason, README counts + failed-resume sentence, live-test local imports. Spec text nits (#10 _TOOL_PARAMS derived, #11 SECURITY.md) → controller edits the spec. Deferred-minor triage: none block merge. ONE fix dispatch (qwen, 90 turns) sent.
- Final fix wave landed 323c3d4 (qwen, 79 turns; 573 unit + 12 docker-live green). Re-review (Sonnet): 9/9 ADDRESSED, no new breakage. Parked (deferred): (a) test_malformed_entry_abort_reports_first_threshold does not distinguish old/new (fix verified by inspection); (b) finalize has no try/finally around export_run for the stash (export_run cannot raise in practice — it catches SandboxError/OSError). Spec deltas committed c1173a7.
- FINAL REVIEW CLEAN after one fix wave. Branch sp2.5-harness head 323c3d4. Workspace record mirrored to docs/superpowers/bench/ and workspace deleted.
