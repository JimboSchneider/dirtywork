# Issue #135 W1: package skeleton (errors, bounds, registration) — run ledger

Plan: [issue #135 plan](../plans/2026-09-08-issue-135-firewall-schema.md), Task 1 / worker brief W1. Base: `3fc17d8` (main, right after PR #157). Branch: `dirtywork/issue-135-task-w1-of-0909162343-c6ecfa5d`.

## Worker setup

PyPI release checked on 2026-09-08: `dirtywork==0.13.1`, invoked through `pipx run --spec`. Image `dirtywork-worker-pytest:0.13`, network disabled.

This run departs from the plan's Invocation block in two ways, both deliberate and both on the record here:

- **Host and model.** Worker: `Qwen3.6-35B-A3B-MLX-4bit` (mlx-community-style 4-bit, 3B active parameters, lmstudio-community weights) served by **oMLX 0.6.4** at `http://localhost:8000`, not `qwen/qwen3-coder-next` on LM Studio. oMLX was installed on 2026-09-09 to compare it against LM Studio for the dirtywork workers; this is its first dogfood run. oMLX settings: burst decode `aggressive`, `max_context_window` 65,536, SSD KV cache on (capped 50 GB, empty at start), prefill priority `speed`, per-model `enable_thinking: false` (the model otherwise emits its reasoning as plain content). LM Studio's resident model was unloaded before the run.
- **Provider path.** `--provider anthropic --base-url http://localhost:8000` with the oMLX API key in `ANTHROPIC_API_KEY`, against oMLX's Anthropic-compatible `/v1/messages`. dirtywork 0.13.1's OpenAI provider sends no `Authorization` header and oMLX's key check was left on, so the Anthropic client was the only authenticated path. `--context-window 65536` passed explicitly because the Anthropic client's window is a static default.

Brief: the plan's `### Worker brief W1` block, passed verbatim as one argv element (146 lines, 5,863 bytes): four new files by `write_file` with BEGIN/END reference blocks, one `edit_file` pair on `pyproject.toml` line 34.

## Result

| Run suffix | Status | Turns | Wall seconds | Prompt tokens | Completion tokens | Completion tokens / wall second | Nudges | Verdict |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `0909162343-c6ecfa5d` | completed | 10 | 134.9 | 66,305 | 2,164 | 16.04 | 0 | accept |

Wall and usage come from the transcript (`run_start` 21:23:44.6Z to `run_end` 21:25:59.5Z). The rate divides completion tokens by whole-run wall time; it is not model decode throughput.

Server-side, from the oMLX log: 10 `/v1/messages` requests, 2,164 completion tokens generated in 24.2 s of decode time, **89.6 tok/s** average decode (range 40.5–114.6; the single 1,316-token `write_file` of the test module ran at 114.6). The remaining ~110 s of wall is prefill of 66k prompt tokens, tool execution in the container, and the in-container verify run of the full suite.

Tool calls: `list_dir` 6, `read_file` 2, `grep` 2, `write_file` 4, `edit_file` 1, `bash` 2, `finish` 1; truncations 0; trimmed turns 0; no tool errors. The worker explored (six `list_dir`, two `grep`) before writing, then wrote all four files and the edit in five consecutive calls, ran the targeted test file and the full suite itself, and finished.

`files_changed` = exactly the brief's five paths. Verify gate (in-container default suite) exit 0: 1,715 passed, 8 skipped.

Sampler (`tools/soak_sampler.sh`, [CSV](2026-09-09-issue-135-w1-sampler.csv)): 27 samples in the run window, free memory 42.19–45.10 GiB (LM Studio empty; oMLX holding the 19.3 GB model). The CSV's `lms_*` columns read 0 for this run by construction; they sample LM Studio, which was idle.

For comparison, the last LM Studio + `qwen/qwen3-coder-next` run on a brief of similar shape (issue #151, 2026-09-08) took 21 turns, 102.6 s and 162,822 prompt tokens at 23.34 completion tokens per wall second.

## Host validation

- All four new files byte-equal to the brief's reference blocks (`cmp`); `pyproject.toml` diff is exactly the brief's one-line edit.
- Full suite, `PYTHONPATH=. pipx run --spec pytest pytest -q -p no:cacheprovider`: **1,714 passed / 9 skipped / 38 deselected**, 112.4 s. Baseline `3fc17d8`: 1,709 passed. Plan expected 1,714.
- `git diff --check`: clean. `ast.parse(..., feature_version=(3, 9))` over `dirtywork/firewall/*.py` and `tests/test_firewall_*.py`: silent.
- Task 1 extra gate: `pipx run build --wheel` succeeded; `import dirtywork.firewall` (plus `bounds`, `errors`) from a clean venv with that wheel installed: ok.

## Preserved receipts

Slug `issue-135-task-w1-of-0909162343-c6ecfa5d`; local receipts at `~/.dirtywork/runs/<slug>/`: `run.json` (with the accept verdict), `transcript.jsonl`, `diff.patch`, and `orchestrator/` (exact brief, launch command and timestamps, run stdout JSON and stderr).
