# SDD ledger — plan: docs/superpowers/plans/2026-08-16-sp2-docker-sandbox.md
Spec: docs/superpowers/specs/2026-08-15-review-response-design.md (Sub-project 2 section)
Branch: sp2-docker-sandbox (from main 5db9e96 = v0.3.0 + site bump). Implementation: dirtywork (v0.3.0 launcher, main checkout) + qwen/qwen3-coder-next per task, worker branch dirtywork/<slug> fast-forwarded into sp2-docker-sandbox after review; per-task review by subagent.
Worker test cmd: `PYTHONUSERBASE=/Users/jimschneider/Library/Python/3.9 python3 -m pytest -q` (pytest lives in user-site; HOME is relocated to the worktree).

## Preflight rulings (SP1-as-merged vs plan's assumed SP1 shapes)
- Ruling: `host_diff_stat` signature is `(worktree, base_commit, cap=64_000)` (SP1 as merged), not `(worktree, cap)` as the plan's Global Constraints state; SP2 code that calls it passes base_commit — why: plan text is stale vs main — cost if wrong: none, tests catch it.
- Ruling: `run_end` also carries `untracked` (host_untracked) in host mode; Docker finalize (Task 11) should surface an equivalent untracked list from the export or explicitly document that the tar export makes it moot — why: parity of the observability contract across sandboxes — cost if wrong: minor doc/field gap.
- Ruling: `workspace.remove_worktree(repo, slug)` exists; SP2 __main__ (Task 12) must call it on any post-worktree preflight failure (docker preflight, run.json, sandbox.start) instead of orphaning — why: SP1 review P1 — cost if wrong: orphaned worktrees on failure.
- Ruling: version target is 0.4.0, not 0.3 (0.3.0 shipped as SP1 on 2026-08-16); Task 14 docs say 0.4 — why: already released — cost if wrong: doc wording only.
- Ruling: `docker` marker refinement over spec's `-m live` accepted as written in Global Constraints.

## Task log

## Preflight scan rulings (see preflight-scan.md; 7 mismatches, 11 drift, 4 spec conflicts, 8 rubric traps)
- Ruling D0/D1 (T2): `HostSandbox.start(worktree, repo, slug, base_commit)` stores base_commit; `finalize()` calls `host_diff_stat(self.worktree, self.base_commit)`; `RunArtifacts` gains `untracked: str = ""`; HostSandbox fills it via `host_untracked` — why: plan's assumed SP1 signature is stale, and run_end.untracked is a shipped 0.3 field — cost if wrong: TypeError at finalize / lost field.
- Ruling untracked-in-docker (T11): DockerSandbox.finalize computes `untracked` IN-CONTAINER (`git status --porcelain --untracked-files=normal`, `??` lines, cap 64k) alongside the in-container diff_stat; no extra host git on worker content — why: parity of run_end contract without violating spec's "only host git after worker output is read-tree HEAD" — cost if wrong: one extra docker exec.
- Ruling D2/D3/D4/D6/D7: plan line-number citations are stale (tools.py grew ~96 lines in SP1); implementers locate blocks by name — cost: none.
- Ruling D5 + T7 read_file regression: Task 7 must NOT replace host `tools.read_file` with `Path.read_text`; keep the SP1 `_open_regular` path and extract only line-numbering into `_number_lines`; `_guard_readable` does not exist and must not be invented — why: security regression (symlink TOCTOU, FIFO hang, unbounded read) — cost if wrong: hardening lost.
- Ruling D9/R2 (T5, T12): `create_worktree` keeps main's body verbatim (incl. `remove_worktree` on post-check failure and `existed`-conditional branch cleanup on add failure), only adding `no_checkout=False`; Task 12 keeps `remove_worktree(repo, slug)` on RunDirError AND calls it on docker preflight/`sandbox.start()` failure before the run begins — why: SP1 P1 fix; existing test pins it — cost if wrong: orphaned worktrees.
- Ruling T2↔T7 list_dir order: DockerSandbox.list_dir sorts entries by raw name BEFORE formatting, matching host — cost: none.
- Ruling R3 (T12): construct `Transcript` before `DockerSandbox` and pass `transcript=transcript`, so sandbox_reset events reach the transcript — cost if wrong: unreachable feature.
- Ruling C4 (T6): `DockerSandbox.start()` wraps any `WorkspaceError` from `validate_objects_dir` as `SandboxError` — status `sandbox_error` — cost if wrong: wrong status bucket.
- Ruling C1/C2 (T7): implement spec fallbacks — probe once, lazily, cached on the instance: `rg` else `grep -rn`; GNU `find -printf` else `ls -1Ap` — cost if wrong: broken tools on custom images.
- Ruling C3/R1 (T11): `host_read_tree` runs only when export_status == "ok"; on failure the host worktree is left as .git-only — cost if wrong: misleading host git status on failed exports.
- Ruling R4 (T14): also update README "Machine contract → Transcript events" paragraph for the docker-mode run_end fields (+ untracked) — cost: stale docs.
- Ruling R5 (T10): add a test that a PAX_FORMAT tar with per-member pax headers (long path) is ACCEPTED while global pax headers are rejected — cost: one test.
- Ruling R6/R7: accepted as-is (defense in depth; idempotent cleanup). R8: Task 2 Protocol docstring documents that tool methods may raise BudgetExceeded/SandboxError.
- Ruling sizing: split Task 7 → 7a (steps 1–7) / 7b (8–15); Task 9 → 9a (1–4) / 9b (5–10); Task 11 → 11a (1–6) / 11b (7–12); Task 12 runs whole with an explicit integration checklist in the dispatch and a heavier review model — cost: extra runs.
- Ruling T12 tests: every pre-existing test_main test that invokes main() gets `--sandbox none` (not just the 4 the plan names) — cost: none.
- Task 1: implementer run completed (branch dirtywork/you-are-implementing-one-task-0816133933-123de357, e0a0b4c, 58 turns, 260 tests). Review: spec ❌, 2 Important (both plan-mandated), 2 Minor.
- Ruling T1 stdin: ACCEPT the plan's `stdin=DEVNULL` default over the original fd inheritance — a worker `bash` command must never read the operator's terminal/pipe (it would block until timeout or swallow orchestrator input); DEVNULL is the correct primitive for both host and docker exec. Not bit-for-bit, deliberately. Pin with a test (`bash -c 'read x; echo got:$x'` returns immediately, output `got:`) and note it in tools.bash's docstring — cost if wrong: none known (no bash tool call legitimately reads stdin).
- Ruling T1 OSError path: REAL regression; fix in tools.bash without changing the plan's `Captured` shape: when `captured.returncode is None and not captured.timed_out` return `f"ERROR: bash failed: {output}"`; add a test that monkeypatches `subprocess.Popen` in dirtywork.procs to raise OSError — cost if wrong: none.
- Task 1 minors → fix round: remove dead `signal`/`threading` imports from tools.py; `argv: list[str]` hint.
- Task 1: fix round 1/5 FAILED to produce a commit (max_turns 40, no edits): worker derailed by guardrail false positive `cd <abs worktree path>` BLOCKED (10 such blocks in the Task 1 run too). Worker branch discarded.
- Ruling tooling: fix the guardrail on branch fix/cd-into-worktree (worktree .worktrees/tool, off main); workers run from that branch's launcher until the user merges it; worker preamble now forbids `cd` and pins the pytest command — why: the tool blocks its own workflow — cost if wrong: none (test-covered; merge is the user's call).
- Task 1 fix round 2/5 aborted immediately: model_error "repeated malformed tool calls" — qwen attached `description` to bash calls → TypeError ×3. Fixed on fix/cd-into-worktree (PR #9, 2nd commit): ToolExecutor drops unknown args. Worker branch discarded. Round 3 next with the same fix text.
- Task 1: fix round 3/5 (4 addressed, 0 open; commits e0a0b4c..ca38066). Deferred minors: dead `original_popen` local in test_bash_popen_failure_returns_error_prefix; redundant `not captured.timed_out` clause in tools.bash.
- Task 1: complete (commits 5db9e96..ca38066, review clean after fix round). sp2-docker-sandbox ff'd to ca38066.
- Task 2: worker run max_turns(60) after work done; commit b0b8ac0 lacked new files (staged); controller amended → b308c49 (no code changes by controller). 269 tests. Review dispatched.
- Task 2 review: spec ❌, 4 Important (repo/slug not stored; RunArtifacts.untracked missing; Protocol raises undocumented; duplicated measure_worktree call [plan-mandated → Ruling: extract _measure(), cost none]), 4 Minor (dead budget import in tools.py, redundant worktree_base_commit call, assert in finalize, untested non-mutating-call budget semantics [deferred]). ⚠️ finalize= wiring to sandbox.finalize() → resolved: Task 12.
- Task 2: minor (deferred): no test that read_file/list_dir/grep skip the budget check.
- Task 2: fix round 1/5 (7 addressed, 0 open; commits b308c49..0c7c0d2). Deferred minor: `_measure() -> dict` annotation should be BudgetReport.
- Task 2: complete (commits ca38066..0c7c0d2, review clean after fix round). sp2-docker-sandbox ff'd to 0c7c0d2. 270 tests.
- PR #9 (guardrail cd-into-worktree + unknown tool args) squash-merged to main as 23a9c22; launcher back to main checkout. NOTE: sp2-docker-sandbox is based on 5db9e96 (pre-#9) — merge main into the branch after Task 3 lands and re-apply the unknown-arg filter to the Task 2 dispatcher if the merge drops it (tools.py conflict expected).
- Owner rule (14:21): keep code DRY & SOLID — added to worker preambles + reviewer constraints; plan-mandated duplication → Ruling: extract helper.
- Task 3: complete (commits 0c7c0d2..d34ba85, review clean; 20 turns). Deferred minors: resolve_image name split mis-parses registry:port refs (plan-mandated); validate_objects_dir git calls lack timeout (plan-mandated).
- Merged main (23a9c22, PR #9) into sp2-docker-sandbox → d44e354; adapted 2 tests to ToolExecutor(HostSandbox(wt)); 299 tests.
- Task 4 review: spec ✅, 1 Important (plan-mandated verbatim duplication worker/export argv → Ruling: extract fragment helpers, argv unchanged), 2 Minor (unused cfg in prep_run_argv → comment; no per-step TDD evidence in report → deferred, inherent to dirtywork runs).
- Task 4: fix round 1/5 (2 addressed, 0 open; commits 08ff51f..6e05af9).
- Task 4: complete (commits d44e354..6e05af9, review clean after fix round). 312 tests.
- Task 5 review: spec ❌, 1 Important (create_worktree rewritten beyond mandate: renames, 2 security comments + attack-scenario message dropped), 2 Minor (import order in test_workspace.py; host_read_tree duplicates _git's subprocess shape → DRY fix). ⚠️ pytest evidence summary-only → controller verified 320 passed.
- Process: fix.sh max-turns 40→60; fix briefs must inline exact target text (no 'git show <sha>' archaeology for the worker).
- Task 5: fix round 1/5 (2 addressed, 1 deferred-minor; commits 2016ba5..e7779de). Deferred minor: test_workspace.py import block has host_read_tree before host_diff_stat (non-alphabetical).
- Task 5: complete (commits 6e05af9..e7779de, review clean after fix round). 320 tests.
- A/B (owner request 14:45): Task 7a brief run twice from the same base (Task 6 head): (1) qwen/qwen3-coder-next via main launcher → task-7a-run.json; (2) glm-4.5-air (loaded at 65536 ctx; launcher .worktrees/tool-glm on throwaway branch ab/glm-context adding the CONTEXT_WINDOWS entry) → task-7a-glm-run.json. Sequential (models don't co-fit comfortably in 128 GB). Both reviewed with the identical Sonnet review prompt; scoreboard rows for both; winner = fewer Critical/Important, tie → fewer turns; winner is landed, loser discarded. Rulings for Task 7 (D5 read_file hardening; list_dir raw-name sort; rg/find fallbacks) are woven INTO task-7a/7b briefs (lesson 2).
- Owner (14:47): add local-LLM metrics to the repo → Ruling: (a) the feature is SP3 §5 `dirtywork bench` — do not bolt on in SP2; carry these bench-outcome categories into SP3 briefs: turns, wall-clock, prompt/completion tokens, status, review verdict (acceptance), and harness-failure classes (guardrail_block count, malformed/unknown-arg tool calls, incomplete commit). (b) commit the scoreboard+lessons as docs/superpowers/bench/2026-08-16-sp2-worker-scoreboard.md on the branch at finish.
- Task 6: worker committed cfbe595 (331 tests) then called unknown tool `finish` 3× → run status model_error. Work complete. Harness follow-up (main): models signal completion with a `finish`/`done` tool; consider a no-op completion tool or treating that name as end-of-run (SP3 ToolSpec registry is the natural place). Review dispatched.
- Task 6 review: spec ✅, 2 Important (plan-mandated: 4× duplicated failure-raise block → Ruling: extract `_check`; bare `except Exception: pass` on tether wait → Ruling: narrow to TimeoutExpired + kill), 4 Minor (per-attempt exec timeout == overall deadline in _wait_ready; _wait_ready deadline path untested; unused DockerError import in test → fixing; start() is one long method → deferred). 
- Task 6: minor (deferred): _wait_ready per-attempt exec timeout equals overall T_LIFECYCLE deadline; _wait_ready deadline-exceeded path untested; start() ~65-line method could be decomposed.
- Research (local-model-research.md): top pick Qwen3.6-35B-A3B (20 GB MLX 4-bit, 262k ctx, SWE-bench V 73.4% self-reported; LM Studio tool-call bugs fixed ≥0.4.5, we have 0.4.21). glm-4.5-air has an OPEN LM Studio tool-calling bug (#829). Ruling: A/B challenger becomes qwen/qwen3.6-35b-a3b (fits alongside qwen3-coder-next → concurrent runs); glm run optional third with ChatML workaround. Download started 14:57.
- Harness robustness follow-ups (candidate PR vs main, "SP2.5"): (1) completion signal — no legal way for a model to say done; treat repeated unknown-tool `finish`-like calls as end-of-turn or add a no-op finish tool; (2) MAX_CONSECUTIVE_FAILURES conflates failure kinds and aborts even after a successful commit; (3) `no tool_calls == completed` is unsafe: tool calls emitted inside <think>/text (Qwen3.6 reports) → false `completed`; guard by detecting tool-call-looking payloads in assistant text; (4) CONTEXT_WINDOWS hard-coded, unknown model → 32k default; make it a flag/env or read from server metadata.
- Task 6: fix round 1/5 (3 addressed, 0 open; commits cfbe595..c99ca20).
- Task 6: complete (commits e7779de..c99ca20, review clean after fix round). 331 tests. sp2-docker-sandbox = c99ca20 = A/B base for Task 7a.
- Task 7a [qwen3-coder-next arm] review: spec ❌, 2 Important (edit_file discards write result — worker defect; _read_raw truncation+lossy decode feeding edit_file — plan-mandated → Ruling: refuse >MAX_READ_BYTES and strict-decode for edit, replace-decode for read (host parity)), 2 Minor. Fix round 1 dispatched on the qwen arm in parallel with the challenger run.
- Process: mirror.sh copies scoreboard/ledger/research into docs/superpowers/bench/ on the branch at each task completion (blog-post record survives workspace deletion).
- 15:32 controller error: qwen-arm fix round never launched (unset shell var → fix.sh unbound arg) and my wait loop stalled ~20 min; redispatched. Qwen3.6 arm finished 23 turns / 2.1 min, 341 tests, but did NOT commit; controller committed its changes verbatim (7746819) for review.
- Harness follow-ups (SP2.5, cont.): (5) stall detection — end run as 'stalled' when N consecutive turns change no file and produce no new test result; (6) resume — restart from an existing worktree with the prior transcript as context (today's max_turns cases were spinning after the work was done, not budget-starved, except Task 2).
- A/B 7a result: qwen3-coder-next 27 turns/1.8m/2I/committed vs Qwen3.6-35B-A3B 23 turns/2.1m/3I (same two defects)/did NOT commit. Ruling: tie on code quality; land the qwen3-coder-next arm (instruction-following + fix round in flight); run Qwen3.6 as second arm on 7b for a second data point. Notable: both models made the identical edit_file deviation → brief-level cause.
- Task 7a: fix rounds 1–2 (3 addressed, 0 open; commits a10971d..f0f0e05). Deferred minors (pending 7b): unused imports in docker.py (Captured/run_capped, check_bash_command, MAX_BASH_CHARS, MAX_LIST_ENTRIES, LIST_EXEC_TIMEOUT).
- Task 7a: complete (commits c99ca20..f0f0e05, review clean after fix rounds). 344 tests.
