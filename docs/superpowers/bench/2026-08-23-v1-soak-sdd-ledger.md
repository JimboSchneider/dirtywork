# SDD ledger — v1 soak (#48)
Matrix: docs/superpowers/bench/2026-08-23-v1-soak-matrix.md (owner-approved decisions 17:50). Branch: v1-soak-48 off main @ 82a353e (0.10.1). Scoreboard: 2026-08-23-v1-soak-worker-scoreboard.md. Raw rows: ~/.dirtywork/bench/soak-{A,Aprime,B,C,D}.jsonl; sampler: ~/.dirtywork/bench/soak-sampler.csv (5 s).

## Pre-flight scan (2026-08-23 17:40–18:04 CDT)
| Item | Found | Action |
|---|---|---|
| LM Studio | only qwen resident, PARALLEL 1 (documented steady state: both models, PARALLEL 4) | reloaded qwen `-c 65536 --parallel 4`; loaded devstral `--parallel 4` |
| Devstral context | `lms load -c 32768` / `--context-length=32768` both ignored: `/api/v0/models` reports `loaded_context_length` 325120 (max 393216) — LM Studio's per-model config wins over the CLI flag | left as-is; harness server-window discovery will see 325k for devstral. **Deviation from CLAUDE.md's 32,768.** RSS stayed 15 GB (KV allocated lazily) |
| Ollama | `qwen3.6` resident with 262144 ctx, llama-server RSS 26.8 GB; free RAM fell to ~150 MB with all three loaded | unloaded (`keep_alive: 0`) until leg C; free RAM back to ~30 GB |
| Worker image | `:0.10` local, RepoDigest == `PINNED_DIGEST` (sha256:4fc400ca…) | — |
| `dirtywork bench` flags | no pass-through for `--verify/--max-tokens/--stuck-repeats/--stall-turns` | tools/soak_driver.py drives `dirtywork run` for legs A′/B/D |
| Existing bench tasks | none can fire F1/F2/F3/F5 | 4 provokers added (bench/repos/); review found 3 model-workarounds (generator script, stub `httpx.py`/test edit, hang announced in task text) → hardened, bypasses verified failing in the real acceptance container |
| invoicr (leg D) | image ships .NET SDK 8.0.424 + empty NuGet cache; invoicr targets net10.0; `network none` blocks restore | owner ruling: custom image `dirtywork-worker-net10:0.10` FROM the pinned digest + dotnet-install 10.0 (+231 MB, 23 s build; invoicr `dotnet test` 33/33 in 9.6 s) and `--allow-network`. Follow-up filed as #59 (1.0 milestone): bump the maintained image to .NET 10 + re-pin |
| invoicr #94 text | stale vs repo: test project + CI `dotnet test` already exist; migrations now timestamp-prefixed | task file carries a "current repo state" note; code wins over plan where they conflict |
| Scripts | `/opt/homebrew/bin/python3` (3.14) lacks pytest + editable dirtywork; `/usr/bin/python3` has both | all soak commands use `/usr/bin/python3` |

## Legs
Leg A: `dirtywork bench` 3 tasks × {qwen, devstral} × 2 — started 18:04:23; net10 image build (18:05–18:06) overlapped runs 1–3 (download-bound; noted for sampler reads). Result 18:07: 12/12 completed, acceptance 12/12, gamed 0; qwen mean 9.8 s / 17.9k tok, devstral mean 26.3 s / 28.8k tok.
Leg A′: driver, `--verify <acceptance> --verify-rounds 2` — started 18:07 inside a 10-min tool window (mistake: leg B would have been cut off mid-run); stopped after 3 rows, no orphan process/container, relaunched detached 18:11 (resumable-by-label skipped the 3 done rows).
Leg B: queued behind A′ in the same detached chain (18 rows).
Leg A′ (18:07–18:10): 6/6 completed, `F4(passed=True,rounds=1)` + F9 on every row; verify round-2 path NOT exercised (no first-round failure) → follow-up rows `F4b-round2-*` (verify command fails its first round by design) queued as leg A2.
Leg B (18:10:58–, v1 provokers), qwen rows first:
- F1 `py-rename-symbol`: FIRED r1+r2 — one `apply_edits` call (8 hunks), 4 turns, ~20 s, acceptance pass.
- F2 `py-impossible-test`: NOT fired (r1 `max_turns` 20 turns/29 s; r2 same 17 s). Transcript: 15 *distinct* install attempts (pip, pip3, `-m pip`, apt, ensurepip, venv…), never the same failing command twice → `RepeatTracker` (same command + same output ×4) has nothing to catch; `ProgressTracker` never idle either. The model also read `acceptance/NOTES.md` and the test docstring, both of which named the design. Provoker replaced by `py-canonical-config` (edit→test loop with an invariant hash-mismatch failure) for leg B2. **Design note for 1.0:** the stuck detector's premise (identical retries) did not match how qwen fails; the cost sink here was `max_turns`.
- F3 `sh-hanging-script`: NOT fired (r1 completed 21 s; r2 13 s). The model read `acceptance/check.sh`, copied its `< /dev/null`, so `read` returned EOF immediately; no bash call ever exceeded its timeout. Provoker replaced by `sh-wait-for-service` (unbounded health-probe loop; hangs regardless of stdin; check.sh carries no reproduction hint).
- F5 `py-big-fixture` @ `--max-tokens 1024`: PARTIAL — truncation surfaced as designed (`finish_reason=length` ×3 → `truncated` nudge ×3) but the model's retry chunk (100 rows) still exceeded the cap and the run ended `model_error` after `MAX_CONSECUTIVE_FAILURES=3` (runner.py:39) — 4 turns, 39 s. **Findings for 1.0:** (a) with LM Studio a truncated tool call comes back as an empty `length` reply, so the spec §1.3 `truncated_call_result` path (which names the file and says nothing was written) was never reached — only the generic text nudge, which does not tell the model the cap size; (b) `model_error` is a misleading terminal status for "three consecutive truncations". Re-running at `--max-tokens 2048` in leg B2 to see whether recovery completes when the chunk fits.
- F5 `py-big-fixture` @ default `--max-tokens 8192` (qwen): **FIRED** — first write truncated (`finish_reason=length` → `truncated` nudge), recovery via `write_file` + 3× `append_file`, 8 turns, 4.6 min, 81.6k prompt / 22.4k completion tokens, acceptance pass. Recovery path proven end-to-end; the 1024 rows document the abort path (`MAX_CONSECUTIVE_FAILURES`).
Review (18:03–18:17, 3 lenses + 1 skeptic per finding, 24 findings → 22 confirmed / 2 refuted): blockers = the F2/F3 provoker flaws found in-run (v2 tasks in progress); tooling fixes dispatched (final_message not persisted → `abort=` never rendered; `|` unescaped in table cells; DRY copies of bench/runs helpers; Ctrl-C leaks; resume edge cases; export wait charged to tool time); bench fixes dispatched (GNU `timeout` absent on macOS → host-side "unsolved" test vacuous; py-big-fixture acceptance accepted any 6-field file); matrix corrected (A′ verify command form; F5 8192 is not an "unprompted" control — the correct file is ~22 KB). Issue filed: #59 (.NET 10 image).
- Re-score note: `py-big-fixture`'s acceptance was hardened mid-leg-B (commit 034c524; qwen rows scored by the lax 6-field checker, devstral rows by the strict per-column one). Re-scoring qwen's F5-default output against the strict checker: **FAIL** — `row 104 col balance: expected '342.99', got '343.01'` (1 arithmetic slip in 400 literal rows). Harness verdict unchanged (F5 fired, recovery completed); task verdict becomes fail. Scoreboard will carry both.
Leg B devstral rows: F1 r1 FIRED (98 s); F2 v1 `max_turns` (30 s) and F3 v1 completed (31 s) — same non-firing reasons as qwen.
- F5 `py-big-fixture` @ 1024 (devstral r1): **cost sink, no guard fired** — 40 turns / 900 s / 194k prompt tok, `max_turns`. Sequence: truncated reply → successful (small) `write_file` → truncated → `write_file` … ×17, never `append_file`. Each successful write resets `FailureTracker`'s consecutive count and counts as progress for `ProgressTracker`; `RepeatTracker` only sees bash. Net effect: the same file was rewritten 17 times without growing and nothing stopped it before `--max-turns`. **1.0 candidate:** a no-net-progress guard for "truncated reply followed by a write to the same path that does not extend it" (or a cumulative-truncation cap), plus a nudge that names the cap size — the current text only says "write in chunks".
- F5 @ 1024 (devstral r2): `model_error` after 139 s — three consecutive truncations this time; r1's 15-min sink vs r2's clean abort differ only in whether a small successful `write_file` landed between truncations.
- F5 @ default 8192 (devstral): **FIRED** — truncation → `append_file` ×4, file reached 400 rows in ~3 min; then the run ended `model_error` (709 s): devstral's verification replies carried literal `[TOOL_CALLS]` markers inside its prose and LM Studio surfaced tool calls whose *name* was the preceding text + `[TOOL_CALLS]bash` → three consecutive unknown-tool failures → abort. **1.0 candidate:** treat an unknown tool name containing a text-tool-call marker as the `text_tool_call` case (re-issue nudge) rather than a generic failure. Strict re-score of its file: FAIL (no trailing newline; id 400 balance 1313.33 vs 1332.00).
Leg B done 18:53:06. v1 provokers `py-impossible-test` / `sh-hanging-script` deleted (superseded by v2; their 8 rows stay in soak-B.jsonl as evidence).
Leg A2 (19:00): `F4b-round2-qwen` **FIRED** the verify retry path — `F4(passed=True,rounds=2)`, 8 turns, 11 s. `F4b-round2-dev`: **0.10 DEFECT** — after `finish()` the failed-verify feedback is sent as a *user* message right after the `finish` tool result; Devstral's (Mistral) chat template on LM Studio rejects it ("conversation roles must alternate user and assistant roles except for tool calls and results") → HTTP 400 → `model_error` (22 s). Every `--verify` retry with a strict-template model via LM Studio is unusable; A′'s devstral rows passed round 1 and never reached it. Fix candidate: deliver verify feedback as the `finish` call's tool result (or as an assistant-then-user pair) instead of a bare user turn after a tool result — runner.py already special-cases "two consecutive user messages" for stall nudges (≈:585), this is the same class. **Issue to file (owner to confirm): "verify feedback after finish() breaks strict chat templates (Mistral/LM Studio)".**
Leg B2 (19:00:41–): `F2v2-canon-qwen-r1` NOT fired — `max_turns` (20 turns / 21 s); qwen never edited config.json, ran the check twice with different flags, then spent 17 turns on git history/find/sha256sum. Six qwen runs across two F2 provokers: zero identical consecutive failures. Devstral v2 rows pending; if they also don't loop, a final pair of rows with `--stuck-repeats 2` will be run to exercise the mechanism at its lowest documented setting, recorded as such.
- `F3v2-wait-qwen-r1`: NOT fired — completed (18 turns, 95 s, acceptance pass) without any bash call exceeding its timeout: qwen only ran the bounded `acceptance/check.sh`, probed curl under its own `timeout 5`, and its one bare `bash -x build.sh | head -50` was ended by SIGPIPE. qwen guards its own commands; combined with F2, the "runaway worker" guards have found no runaway in qwen. Leg B3 queued: F2 at `--stuck-repeats 2` and F3 with a task that instructs the worker to run `build.sh` — mechanism checks, labelled as provoked-by-instruction.
- qwen B2 rows: `F2v2-canon-qwen-r2` `max_turns` (35 s; 0/7 identical repeats across all F2 rows); `F3v2-wait-qwen-r2` completed (20 turns, 134 s, no `timed_out`; 0/4 on F3); `F5-trunc2048-qwen-r1` `model_error` (75 s, 3× `truncated`, no write landed — chunks still ≈2k tokens).
- `F5-trunc4096-qwen-r2`: `model_error` (147 s) — 3× `truncated`, 12.3k completion tokens burned, no write landed; after each nudge qwen announced "header and first part" and still overran 4096. F5 on qwen: 8192 → recovers (4 chunks); 4096/2048/1024 → abort. The cap-blind nudge is the lever: the model cannot size a chunk it isn't told the budget for.
- `F2v2-canon-dev-r1`: `max_turns` (48 s) — 16 bash calls, ONE failing (`unittest`), then git archaeology (`log --all`, `reflog`, `fsck --lost-found`) for a "canonical" copy; max identical-failure streak 1. Both models investigate instead of retrying. B3's F2 rows changed from `--stuck-repeats 2` (cannot fire on a streak of 1) to an instruction-driven variant — "after every change re-run `bash acceptance/check.sh` until PASS" — at the default 4, as a realistic user instruction.
- `F3v2-wait-dev-r1`: completed (17 turns, 134 s), no `timed_out` — F3 0/6 on the neutral task across both models.
- `F5-trunc2048-dev-r1`: **second trigger of the strict-template defect** — devstral emitted 38 `append_file` calls in ONE turn (one CSV row each; all 38 executed), then a `length`-truncated reply with empty text and no tool call; the harness sent the `truncated` nudge as a user message after that empty assistant turn → Mistral template on LM Studio: "roles must alternate…" → HTTP 400 → `model_error` (147 s). Same class as the verify-feedback failure: a harness user-message after a turn the template does not count as an assistant turn (empty content / tool result). Fix candidate: never follow an empty assistant reply or a tool result with a bare user message — give the truncated turn placeholder assistant content, or deliver the nudge as a tool result. Issue draft updated to cover both triggers.
- `F2v2-canon-dev-r2` `max_turns` (47 s; F2 0/10 on neutral tasks); `F3v2-wait-dev-r2` completed (28 turns, 264 s, no `timed_out`; F3 0/7); `F5-trunc4096-dev-r2` `model_error` (412 s, 3× `truncated`, one small `write_file` landed, 12.8k completion tokens). Leg B2 done 19:27:16.
Legs B3 → C → D launched 19:27:25 as one detached chain (`scratchpad/run-rest.sh`): B3 = instruction-driven F2/F3 rows; C = `dirtywork bench --models qwen3.6@ollama` after `keep_alive` reload; D = invoicr #94 on `dirtywork-worker-net10:0.10` with `--allow-network --verify "dotnet test api/Invoicr.Api.Tests" --verify-rounds 2 --max-turns 60 --timeout 3600`.
Leg B3 (19:27–): `F2v2-iterate-qwen` `max_turns` (24 turns, 70 s) — told to re-run the check after every change, qwen made ZERO edits (recognised a hash target cannot be guessed) and investigated; F2 is not a qwen failure mode. `F3v2-run-qwen`: **F3 FIRED** — bare `bash build.sh` → `timed_out: true` after 120 s → `nudge kind=timeout`; the model then read build.sh and diagnosed the health probe correctly (and tried to stand up a python http.server). Run then ended `model_error` on a **new finding**: qwen passed the bash `timeout` parameter as a duration string (`"30s"`, `"60s"`, `"60s"` — corrected from the transcript; plain `"60"` has coerced since 0.6) three times → `bad arguments for bash: parameter 'timeout' must be integer, got str` ×3 → consecutive-failure abort. **1.0 candidate:** accept duration suffixes and include the accepted form in the error so one malformed arg does not cost the run (done in #64, see "#64 evidence" below).
- `F2v2-iterate-dev`: `max_turns` (121 s) — 0 edits, 4 failing bash calls all different, max identical streak 1. **F2 closed: 0/12 across two provokers × two models, with and without an explicit "re-run until PASS" instruction.** Neither model exhibits the identical-retry loop `RepeatTracker` guards against; the code path stays covered by unit tests only. 1.0 note: the guard is cheap and harmless, but the real cost sinks observed were `max_turns` after long investigations and the F5 rewrite loop — a budget/progress guard would catch more than a repeat guard.
- `F3v2-run-dev`: **F3 FIRED** (bare `bash build.sh` → `timed_out: true` at 120 s → `nudge kind=timeout`) and then the **third trigger of the strict-template defect**: the timeout nudge is a user message directly after the timed-out tool result → Mistral template 400 → `model_error` (134 s). Every harness nudge/feedback path that follows a tool result (verify feedback, truncated-after-empty, timeout nudge) is broken for strict-template models via LM Studio. One root cause, one fix (never emit a bare user turn after a non-assistant turn).
Leg C (19:35): misfire — plan used `qwen3.6@ollama`; the Ollama provider requires the exact tag and preflight refused all 3 rows in 0.1 s each with a clear message ("model 'qwen3.6' not available (…qwen3.6:latest). Pull or run it first… Ollama model ids include the tag"). Good harness behaviour (no run spent). Doc nit: the message says "loaded:" but lists `/api/tags` (available), not `/api/ps` (resident). Corrected leg C (`qwen3.6:latest@ollama`, `soak-C2.jsonl`) queued to start after leg D exits, to keep Ollama and LM Studio off the GPU at the same time.
Leg D started 19:35:36 (`D1-issue94-qwen`).
Leg D attempt 1 (`D1-issue94-qwen`, 19:35–19:37, killed by me, exit −15): qwen read the plan section in chunks (tool-aware read fine), surveyed entities/DbContext/DTOs/tests, then `dotnet build` failed — NuGet restore hit **"No space left on device"**: the sandbox mounts `/home/worker` as a 256 MB tmpfs (`docker_args.py:42 home_size="256m"`, no CLI flag; `--tmp-size`/`--gitdir-size` exist, `--home-size` does not) and NuGet's default cache is `~/.nuget/packages`; the .NET first-run also wrote an HTTPS dev cert into the home. The model reacted sensibly (df/du/inodes, restore retry) but the sandbox cannot be fixed from inside. **Findings:** (a) a per-project package cache does not fit the default home; the derived-image pattern must also carry the cache (`NUGET_PACKAGES=/opt/nuget`, pre-restored) — being rebuilt now; (b) 1.0 candidate: expose `--home-size` (or document `home_size`) alongside `--tmp-size`, and have `machine-contract.md`'s custom-image section mention package caches. Leftover worktree `invoicr/.worktrees/dw-invoicr-94-…-49920796` to be cleaned with dirtywork's own run cleanup at the end. Corrected leg C (`C2`, Ollama `qwen3.6:latest`) started 19:37:32; the image rebuild overlaps it (CPU/network only).
Leg C2 (19:37:43–19:38:28): **F7 FIRED** — `dirtywork bench --models qwen3.6:latest@ollama`: 3/3 completed, acceptance 3/3, gamed 0, mean 14.9 s / 18.2k tok; `run.json`: `provider=ollama`, `context_window=262144`, `context_window_source=provider:ollama:server` (`/api/ps` discovery), image pinned. Ollama unloaded afterwards (`keep_alive: 0`).
Image rebuilt 19:44 (`dirtywork-worker-net10:0.10`, 946 MB, +586 MB over base; build 29 s): `NUGET_PACKAGES=/opt/nuget` pre-restored for Api/Api.Tests/ServiceDefaults (452 MB), `DOTNET_CLI_HOME`/`HOME` → `/opt/dotnet-cli-home`, first-run/telemetry off. Offline proof with the sandbox's 256 MB home tmpfs and `--network none`: `dotnet test` 33/33 in 4.4 s, home empty afterwards. Worker finding: `DOTNET_CLI_HOME` alone does not move ASP.NET Data Protection keys, the X509 store or NuGet's migration marker — those read raw `HOME`. Caveat: dirtywork sets `-e HOME=/home/worker` at run time (`docker_args.py:125`), overriding the image's `HOME`, so those few-KB files will land in the tmpfs home during the soak (harmless). Leg D2 (`D2-issue94-qwen`, offline, no `--allow-network`) launched 19:44.

## Findings register (running; feeds the 1.0 issue list)
| # | Kind | Finding | Evidence | Proposed |
|---|---|---|---|---|
| S1 | **0.10 defect** | Harness user-turns after a non-assistant turn (verify feedback after `finish`, `truncated` nudge after an empty `length` reply, `timeout` nudge after a timed-out tool result) → Mistral strict template on LM Studio returns 400 → `model_error` | A2 `F4b-round2-dev`; B2 `F5-trunc2048-dev-r1`; B3 `F3v2-run-dev` | Never emit a bare user message after a tool result / empty assistant turn: fold feedback into the tool result (finish / timed-out call) or give the empty turn placeholder content. **#60** filed. |
| S2 | 1.0 design | Truncation nudge is cap-blind; models cannot size chunks. Recovery succeeds only when the self-chosen chunk fits (8192 ok; 4096/2048/1024 abort) | B `F5-*`, B2 `F5-trunc*` (qwen + devstral) | Put the cap (and the observed overrun) in the nudge; consider a cumulative-truncation stop. **#65** filed. |
| S3 | 1.0 design | No net-progress guard: truncated reply → small successful `write_file` → truncated … resets every tracker; 40 turns / 15 min / 194k tok burned | B `F5-trunc-dev-r1` | "Rewrote the same path without growth" or budget-based guard. **#66** filed. |
| S4 | 1.0 design | `RepeatTracker` (identical failing command ×N) never fired: 0/12 across two provokers, two models, with and without a re-run instruction. Real cost sinks were `max_turns` after long investigations | B/B2/B3 F2 rows | Keep (cheap), but 1.0 cost protection should be budget/progress-based. |
| S5 | 1.0 usability | Bash `timeout` given as a string (`"60s"`) → `bad arguments` ×3 → abort; error names the type but not the accepted form | B3 `F3v2-run-qwen` | Coerce numeric strings; error text `timeout: integer seconds ≤ 600`. **#64** filed. **Fixed on `issue-64-timeout-coercion` (built by dirtywork 0.10.1 + qwen) — see "#64 evidence" below.** |
| S6 | 1.0 usability | Unknown tool name containing `[TOOL_CALLS]` (devstral text markers) treated as a generic failure ×3 → abort | B `F5-default-dev` | Classify as `text_tool_call` and re-issue nudge. **#67** filed. |
| S7 | 1.0 sandbox | `home_size` 256 MB tmpfs has no CLI flag; package caches (NuGet) cannot fit; only `--tmp-size`/`--gitdir-size` exposed | D1 | Expose `--home-size`; document package caches in the derived-image section of machine-contract. **#63** filed. **Fixed on `issue-63-home-size` — see "#63 / #59 evidence" below.** |
| S8 | 1.0 image | Worker image ships .NET 8; invoicr is net10 | pre-flight | **#59** filed. **Fixed on `issue-63-home-size` — see "#63 / #59 evidence" below.** |
| S11 | **0.10 defect** | Sandbox reset on "stray process after bash" wipes tmpfs `/gitdir` silently; a worker's `git stash` (and any refs/index state) is lost between two bash calls, and .NET build daemons trigger a reset after every `dotnet` call | D3 (6 resets; Program.cs registration lost → deliverable incomplete though verify green) | Tell the worker on reset; keep worker git state on the volume or document `git stash` as unsafe; don't reset on known build daemons / set the .NET no-server env in .NET images. **#61** filed → spec `2026-08-25-sandbox-strays-gitfile-and-reset-notices-design.md`, built 2026-08-25 (rows below). |
| S9 | doc nit | Ollama preflight message says "loaded:" but lists `/api/tags` (available) | C | Wording. |
| S10 | doc nit | `lms load -c/--context-length` ignored by LM Studio per-model config; devstral ran at 325k | pre-flight | CLAUDE.md/operating note. |
Leg D2 (`D2-issue94-qwen`, 19:45–19:48): `max_turns` (60) in 2.8 min — **the work largely got done**: 9 files changed (timestamped `supabase/migrations/20260823180000_billing.sql`, `Entities.cs`, `AppDbContext.cs`, DTOs, `ProfileController.cs`, three new test files), 1.90M prompt / 8.0k completion tokens (≈32k prompt per turn against a 65k window, `trimmed_turns` 0, ~11k prompt tok/s). It never reached `finish()`/verify because `dotnet build` failed on **image permissions**: dirtywork execs the worker as the host uid (501:20), the baked `/opt/nuget` + `/opt/dotnet-cli-home` were owned by uid 1000 → "Failed to read NuGet.Config due to unauthorized access"; the model burned ~10 turns on `HOME=/tmp/...`/`cp` workarounds. Image patched (`chmod -R a+rwX`), re-verified offline as uid 501. **S7 addendum:** machine-contract's derived-image guidance should say the worker runs as the *host* uid, so baked caches must be world-readable/writable. Next: `dirtywork resume` with feedback (F10) on the same worktree.
Leg D2b (19:49:25–19:50:08): **F10 + F4 FIRED on a real task** — `dirtywork resume <D2 slug> --feedback "…environment fixed; build, test, finish…"`: 9 turns, 43 s, `dotnet build` + `dotnet test api/Invoicr.Api.Tests` green inside the sandbox, verify gate passed (exit 0), status `completed`, final summary lists the migration (timestamp scheme), `BillingSubscription` + `xmin` mapping, `Profile.Plan`, DTOs, controller sites and 3 test files. Worktree: `invoicr/.worktrees/dw-invoicr-94-…-cd825a30`. Nothing merged. #97 next, on a scratch clone of that branch.
F6 (atomic writes, passive): scanned the invoicr worktree, all 63 run dirs from today, and every exported `diff.patch` for `.dw-tmp.*` (`tools.py:47-48`): **0 leftovers** across all soak runs, including the truncation-abort and max-turns runs that ended mid-write.
Leg D3 (`D3-issue97-qwen`, #97 RequirePro policy) launched 19:51:10 on a scratch clone of invoicr carrying #94's patch as commit d67c893 (`scratchpad/invoicr-97`); Jim's checkout untouched. Independent review of the #94 diff dispatched (Sonnet, read-only) for the scoreboard's Review verdict column.
Leg D3 (`D3-issue97-qwen`, 19:51:10–19:53): status `completed`, verify green (43 turns, 1.9 min, 1.0M prompt tok), files: `Auth/PlanAuthorization.cs` + `Tests/Auth/PlanAuthorizationHandlerTests.cs` — **but the `Program.cs` policy registration is missing**, so #97's deliverable is incomplete despite the green gate. Cause (transcript lines 37–84): six `sandbox_reset` events, reason "stray process after bash" — every `dotnet build/test` leaves MSBuild/VBCSCompiler build-server daemons behind, the harness kills the container, and the tmpfs `/gitdir` is wiped while `/work` survives. The worker ran `git stash` (t35) to test the baseline, `dotnet test` (t36) → reset → `git stash pop` (t37): "No stash entries found" — the stash lived in `/gitdir`. The untracked new files survived on `/work`; the tracked-file edits were lost, and the model concluded "Program.cs shows it was reverted". **S11 (0.10 defect, real work lost):** (a) resets silently destroy worker-created git state (stash, index, refs) — at minimum the worker must be told a reset happened and what it loses; `git stash` should be documented as unsafe or the stash/refs preserved on the volume; (b) the stray-process policy fires on legitimate build daemons — a .NET-bearing image should set `DOTNET_CLI_USE_MSBUILD_SERVER=0`, `MSBUILDDISABLENODEREUSE=1`, `UseSharedCompilation=false` (or the policy should terminate strays without a full reset). D2/D2b had the same resets (counts below) but lost nothing because they never stashed.
- S11 addendum: "stray process after bash" resets also hit 7 of 59 non-.NET runs (py-fix-off-by-one ×2, sh-fix-script, py-rename-symbol, sh-wait-for-service ×3 — the last plausibly from a backgrounded `build.sh`/`sleep`), one reset each. The `sandbox_reset` event records only the reason, not the process list, so these cannot be triaged from the transcript — 1.0: log the stray processes (name/cmdline) in the event.
Cleanup 19:56: `dirtywork runs clean --keep-transcript --force` on every non-invoicr run from today (volumes/worktrees removed, transcripts kept); the four invoicr runs and the `#94` worktree (`invoicr/.worktrees/dw-invoicr-94-…-cd825a30`) are kept for review.
#94 independent review (Sonnet, read-only, 19:56): **Needs changes** — High: `updated_at` trigger missing; `xmin` mapped as a real `uint` property (plan Step 10 required a shadow property; InMemory hides it, Postgres would reject). Medium: no `BillingResponse` test; tautological entity tests. Low: comment/policy-name mismatch; `Plan` positional placement. Host `dotnet build` clean, `dotnet test` 40/40. Recorded on the scoreboard; nothing merged.
Sampler stopped 19:56 (1,296 samples): free RAM min 0.0 / median 8.0 / max 32.9 GB; both LM Studio models resident throughout.
**Soak complete 19:58.** 63 runs (12 A, 6 A′, 2 A2, 18 B, 12 B2, 4 B3, 3 C + 3 C2, 4 D incl. one resume). Feature tally on the scoreboard; findings S1–S11 above. Owner rulings 20:01: S1 → #60, S11 → #61 (both 1.0 milestone); PR opened for `v1-soak-48`; #94 worktree and #97 scratch clone kept for review, unmerged.

## 1.0 roadmap (owner-approved 2026-08-23 21:06, ordered by likely user pain)
1. **#60** — harness user-turn after tool result / empty reply breaks strict templates (verify retry, timeout nudge, truncation nudge). Takes out a whole model family on advertised paths.
2. **S7 / #63** — 256 MB home tmpfs vs package caches; expose `--home-size`, document caches for derived images (+ .NET-in-image guidance, #59). — **DONE on `issue-63-home-size` (PR pending; #59 stays open until the 1.0 image publishes).**
3. **S5 / #64** — coerce numeric-string bash `timeout`; error names the accepted form. — **DONE (PR pending; first item built by the released dirtywork against itself).**
4. **#61** — sandbox reset wipes `/gitdir` silently; tell the worker, preserve/document `git stash`, don't reset on build daemons, log stray processes.
5. **S2 / #65, S3 / #66** — cap-aware truncation nudge; no-net-progress guard for the rewrite loop.
6. **S6 / #67** — `[TOOL_CALLS]`-polluted tool names → `text_tool_call` nudge, not a generic strike.
Then polish: S4 (budget/progress guard over repeat guard), S9/S10 doc nits. All roadmap items are filed (21:10): #60, #63, #64, #61, #65, #66, #67.

## PR #62 review round 1 (owner, 20:49) → fixes
Findings: (1) P1 root pytest collected `bench/repos/*/tests` (CI red); (2) P1 `py-canonical-config` gameable — check.sh ran the mutable test file, only `acceptance/` hashed; (3) P2 F4 reported for `passed: false`; (4) P2 F5 accepted any later `append_file` after any earlier `length`. All four verified real before fixing. Fixes (807fbbf): `testpaths`/`norecursedirs`; canonical sha256 gate embedded in the hash-protected check.sh + `tests/test_config.py`/`app.py` (and py-rename-symbol's `tests/test_ledger.py`) added to `acceptance.hashes` — tampering now scores `gamed` at the hash-check stage; F4 requires `passed is True` (else `F4-failed(rounds=N)`); F5 requires `length` → `truncated` nudge / `truncated_call_result`, then a later `append_file` to the truncated call's path or a `write_file`'d path. Root pytest 1231 passed. Regenerated scoreboard: feature tokens identical (no `verify_failed` runs existed; real F5 rows re-detect).
Adversarial verify (4 skeptics): fixes 1–3 not refuted (canonical gate survived `sitecustomize`/`hashlib`/`unittest` shadows, cwd `python3`, PYTHONPATH injection, symlinked/dir `config.json`; F4 edge inputs all fall to not-fired). F5 refuted on two false negatives — the truncation marker can be a later `tool_result` of a multi-call `length` turn (runner.py:704-810 emission order), and paths recovered from `tool_result.args` break at the runner's 500-char cap — fix in progress (scan to the next assistant turn; take paths from the result text).
Process slip (mine): two `commit -am` ledger commits swept the implementer's in-progress edits; split into a ledger commit and the fixes commit before pushing (6c2f082, 807fbbf).
F5 detector patched (dfa4e9b): scans the whole `length` turn (to the next assistant event) for the truncation marker; paths come from the runner's success strings with `_recovered_path(args)` as fallback. +4 tests; root pytest 1235 passed; real runs re-checked (F5-default-{qwen,dev} F5; abort and pre-truncation-append runs not). Second skeptic pass on the patch before push.
Second skeptic pass on the F5 patch: 8/8 probes hold (multi-result turns, capped args, overwrite/"diff omitted" shapes, spaced paths, two truncations); one further false negative — the path named inside `truncated_call_result`'s own ERROR text was not parsed — fixed in 4919738 (+1 test). Root pytest 1236 passed. Pushed to PR #62 21:33.
PR #62 review round 2 (owner, 21:42): P2 — F5's `written_paths` was precomputed over the whole transcript, so a `write_file` after an `append_file` could retroactively justify it. Fixed: the set accumulates during the scan (a write vouches only for appends that follow it); +1 regression test (write-after → no F5, write-before → F5). Real runs unchanged (F5-default-{qwen,dev} F5; abort and pre-truncation-append runs not). Root pytest 1237 passed.

## #60 reruns (branch issue-60-followup-carriers, 980095c)

| label | model | status | wall_s | turns | nudges (via) | follow_up events | F-detectors | note |
|---|---|---|---|---|---|---|---|---|
| F4b-round2-dev-60 | devstral | completed | 28.2 | 12 | 0 (verify feedback via=finish_result ×1) | 0 | F4(passed=True,rounds=2), F9 | was model_error (jinja 400) at b94dec9 — now completed; verify feedback folded into finish()'s own tool result, no bare user turn |
| F4b-round2-qwen-60 | qwen | completed | 9.8 | 6 | 0 (verify feedback via=finish_result ×1) | 0 | F4(passed=True,rounds=2), F9 | control: F4(passed=True,rounds=2) as expected |
| F5-trunc2048-dev-60 | devstral | model_error | 195.7 | 7 | 3 (user×2, undelivered×1) | 0 | F9 | was model_error (empty reply after 38 append_file) at b94dec9 — still model_error but root cause is now "3 consecutive empty_reply failures" (2048-token cap too small for any chunk at this task's row width); the #60 defect itself does not reproduce (empty replies get placeholder="[empty reply]", no bare user-after-empty-reply 400). Cross-ref #65/#66 |
| F5-trunc2048-qwen-60 | qwen | model_error | 76.3 | 4 | 3 (user×2, undelivered×1) | 0 | F9 | control — identical failure mode ("3 consecutive empty_reply failures") to the dev row and to qwen's b94dec9 control; pre-existing cap-blind truncation limit, unrelated to #60. Cross-ref #65/#66 |
| F3v2-run-dev-60 | devstral | model_error | 240.8 | 18 | 1 (tool_result×1) | 1 | F3, F9 | was model_error (timeout nudge after tool result) at b94dec9 — #60 defect confirmed FIXED: timeout nudge now delivered via=tool_result, folded into the bash tool's own result (no bare user turn, no jinja/400 anywhere in the transcript). Run instead runs 18 turns past the nudge and aborts on "3 consecutive unknown_tool failures" from devstral's own `[TOOL_CALLS]`-polluted tool names (43 `[TOOL_CALLS]` markers across 5 transcript lines) — pre-existing, unrelated to #60. Cross-ref #67 |
| F3v2-run-qwen-60 | qwen | completed | 265.0 | 22 | 1 (tool_result×1) | 1 | F3, F9 | control — completed cleanly; qwen stood up a python http.server to satisfy the health probe. self-verified: the model's own poll-and-check mirrored acceptance/check.sh and printed PASS (no --verify on this row) |

§10's acceptance bar as literally written (no `model_error`, empty `run_end.error` on the three `-dev-60` rows) is **not** met — three rows still end `model_error` for the unrelated causes noted per row; the #60-specific bar is: The #60 criterion is met: across all six reruns, zero occurrences of `Error rendering prompt` / "roles must alternate" anywhere in any transcript, every *delivered* `nudge` event carries `via` (an undelivered nudge recorded on a turn where the run aborted before sending correctly carries no `via`, per spec §6.2's sparse-via ruling), and Devstral proceeds past all three formerly-fatal points (verify feedback after `finish`, the timeout nudge after a tool result, and the empty reply after a tool turn) without a single strict-template 400. The three residual `model_error` rows (both F5-trunc2048 rows, F3v2-run-dev-60) fail for causes unrelated to #60 and already tracked on the 1.0 roadmap: the 2048-token truncation cap is too small for either model's chunking strategy (#65/#66), and devstral's `[TOOL_CALLS]`-polluted tool names still burn through the unknown-tool strike limit (#67).

Placeholder imitation: none observed. F5-trunc2048-dev-60 had 1 assistant turn carrying `placeholder="[empty reply]"` (turn 7, the run's last event — no subsequent assistant turn to imitate it). F5-trunc2048-qwen-60 had 2 such turns (turns 2 and 4); turn 2's placeholder was followed by turn 3's genuine, non-empty reply ("I'll create the `fixtures/rows.csv` file with exactly 400 d...") with no literal `[empty reply]` text, and turn 4 (also the run's last event) had no following turn. Zero assistant turns anywhere in the six reruns contain the literal string `[empty reply]` outside the placeholder field itself.

## #63 / #59 evidence (branch `issue-63-home-size`, 2026-08-24)

**S7 / #63 — D1 restore rerun (measurement, no model in the loop).** The exact leg-D1 restore (`dotnet restore api/Invoicr.Api.Tests/Invoicr.Api.Tests.csproj`, ProjectReferences to Api and ServiceDefaults; AppHost skipped as in the soak image) run under dirtywork's own container flags (`--read-only`, host uid, `/tmp` 1g exec tmpfs, `ulimit -f 524288`, `--memory 4g`) on the two-SDK image with `--network bridge` (= `--allow-network`):

| `/home/worker` | result | `$HOME` used at the end |
|---|---|---|
| 256m (0.10 default) | `NuGet.targets(198,5): error : No space left on device` after 3 s — the D1 error verbatim; tmpfs 256M/256M | 256 MB (140 MB of `~/.nuget` landed before it filled) |
| 1g (`--home-size 1g`) | restore ok, 3 s | **562 MB** (445 MB in `~/.nuget`) |

So the invoicr restore needs ≈ 560 MB of `$HOME`; `--home-size 1g` is the working value, and the flag's plumbing (argv → tmpfs → `run.json`) is proven by `test_docker_live_home_size_flag_caps_home_tmpfs` (`df -B1` inside the container at `--home-size 300m` = 314572800). Docs now say caches live under `$HOME`, that env is fixed at `docker create` (why D1's `HOME=/tmp` could not stick), and that tmpfs is charged to `--memory` (600 MiB write into a 1g tmpfs under `--memory 256m` → OOM-killed).

**S8 / #59 — two-SDK image.** `docker/Dockerfile` installs SDK 8.0.424 + 10.0.400 (one layer, 360 MB → 591 MB, +221 MiB); offline `dotnet new/build` of net8.0 and net10.0 pass in the sandbox; the published `:0.10` cannot even scaffold net10.0. Unreleased 1.0 source — `DEFAULT_IMAGE`/`PINNED_DIGEST` untouched; #59 stays open until the 1.0 image publishes (`PINNED_DIGEST = None` on the first push of the minor, pin at 1.0.1).

**New 0.10 defect found by the live test — the .NET 8 runtime vs `ulimit -f`.** The bash tool runs every command under `ulimit -f 524288` (docker.py); with W^X enabled the .NET 8 runtime trips `RLIMIT_FSIZE` at startup — still at an 8 GiB limit, consistent with the size it gives its double-mapping file — so on the published `:0.10` image every managed .NET process (`dotnet new/restore/build/test`, a built app) dies with `File size limit exceeded` (SIGXFSZ, exit 153); only `dotnet --version` (native muxer) survives. The .NET 10 runtime does not trip it, which is why the soak's SDK-10 derived image looked fine — until a built net8.0 app was run on the two-SDK image (153). `DOTNET_EnableWriteXorExecute=0` fixes both runtimes (verified on both images: new + build + run of net8.0 and net10.0 pass under the limit; GC/tiering knobs do not help); it is now `ENV` in the Dockerfile, and `test_docker_live_dotnet_builds_and_runs_offline[net8.0|net10.0]` guards it in CI. Retro-note on leg D: D2/D2b/D3's `dotnet build`/`test` ran on the SDK-10 runtime, so they never hit this; a net8.0 test host would have.

## #64 evidence — first 1.0 item built by the released dirtywork against itself (2026-08-24)

Owner rule (14:53): every dirtywork change is implemented by the **released** CLI (`pipx run --spec dirtywork==0.10.1`; the pipx install at `~/.local/bin` was a stale 0.7.0) with a local worker, Claude planning and reviewing. The maintained image has no pytest, so the run used a derived image (`dirtywork-worker-pytest:0.10` = pinned base + `python3-pytest`; dirtywork's own suite passes inside it, 1360 in 48 s) — the derived-image recipe from #63, dogfooded.

Facts the task rested on: the soak's three rejected values were `"30s"`, `"60s"`, `"60s"` (run `run-bash-buildsh-from-the-0823192835-8c56b520`) — plain `"60"` has coerced since 0.6, so the gap was the duration suffix and the error text. The changed-args abort exemption from the issue was **declined** (a still-broken model would run to `max_turns`; the first error now names the form).

| run | model | status | wall | turns | s/turn | prompt tok | completion tok | prompt tok/s | nudges | resets | tool errors | tool mix | verify |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `issue-64-the-bash-tool-0824145420-2241a719` | qwen3-coder-next (LM Studio, 65k) | `max_turns` (work complete, no `finish`) | 287 s | 60 | 4.8 | 1,764,839 (≈29.4k/turn) | 8,586 | ≈6.1k | 0 | 3 (stray process after bash — the suite spawns processes; #61) | 3 (2× `read_file offset … got float`, 1× `edit_file` miss) | bash 40 / read_file 11 / edit_file 8 / grep 1 | not reached |
| `issue-64-the-bash-tool-0824150245-d72ee672` (resume 1, Claude review feedback: 8 items) | qwen3-coder-next | `max_turns` — reached `finish` twice, `--verify` failed both rounds (S13, not #64) | 361 s | 40 | 9.0 | 685,463 | 4,693 | ≈1.9k | 0 | 0 | 1 (absolute `/work/...` path to `read_file`) | bash 28 / read_file 4 / edit_file 4 / append_file 1 / grep 1 / finish 2 | ran ×2, failed on `GIT_DIR` hijack |
| `issue-64-the-bash-tool-0824151215-73cb04f8` (resume 2, the 7 small items + git-clean verify command) | qwen3-coder-next | `max_turns`, no `finish` | 202 s | 30 | 6.7 | 678,797 | 3,279 | ≈3.4k | 0 | 0 | 0 | read_file 20 / edit_file 7 / bash 2 / grep 1 | not reached |
| `issue-64-the-bash-tool-0824153944-3e25d65c` (resume 3, adversarial-review findings: bounded digits + re.ASCII, ParamSpec docstring, generic unit test, runner-test hygiene, .gitignore) | qwen3-coder-next | **`completed`** — `finish` reached, `--verify` (git-clean) **passed round 1: 1386 passed in 48.7 s inside the sandbox** | 281 s | 27 | 10.4 | 394,137 | 4,350 | ≈1.4k | 0 | 0 | 1 (`edit_file` miss) | bash 12 / read_file 7 / edit_file 7 / finish 1 | **passed** |
| `issue-64-the-bash-tool-0824155704-a06546cb` (resume 5, PR review P1+P2: wire type `["integer","string"]`, shared `_coerce_param` for `canonical_args`) | qwen3-coder-next | **`completed` with NO change** — called `finish` on turn 1; `--verify` "passed" on the untouched tree | 62 s | 1 | 62 | 6,847 | 497 | — | 0 | 0 | 0 | finish 1 | passed (vacuous) |
| `issue-64-the-bash-tool-0824155850-66d0c6e2` (resume 6, same feedback + an explicit "do not finish until git diff shows changes" guard) | qwen3-coder-next | `max_turns` — partial: `_coerce_param` + schema type array landed, fixture/tests/one call site not reached | 78 s | 30 | 2.6 | 704,897 | 2,764 | ≈9.0k | 0 | 0 | 1 (`edit_file` miss) | read_file 19 / bash 7 / edit_file 4 | not reached |
| **total (6 runs)** | | | **1271 s** | **188** | | **4,234,980** | **24,169** | | 0 | 3 | 6 | | |

Diff at export: `toolspec.py` +54 (`ParamSpec.unit`, `_coerce_duration`, unit branch in `_validate_args`), `builtin_tools.py` +3, `tests/fixtures/tool_schemas.json` regenerated (the worker found the golden schema fixture on its own), `tests/test_toolspec.py` +132 — host suite 1381 passed. Claude review → `resume --feedback` (F10) with eight items (comment placement, regex next to its helper, stronger assertions, runner-level replay of the three soak calls) and the instruction to reach `finish` so `--verify` runs. Resume 1 delivered the runner test and reached `finish`; `--verify` failed on S13 (below), and the seven small items were skipped. Resume 2 (small items only, `env -u GIT_DIR -u GIT_WORK_TREE` verify) applied five of seven and ran out of turns reading files (20 of 30 calls were `read_file`). Claude finished the last two by hand (the comment's placement, trailing whitespace) plus the docs. A light adversarial pass (two Opus lenses) then found a real defect in the worker's code — `int()` on an unbounded digit run raises `ValueError` on Python ≥ 3.11 and would escape `execute()` and abort the run as `model_error` (invisible on this 3.9 box) — plus `\d` in Unicode mode, a missing `ParamSpec.unit` docstring, runner-test hygiene and 798 MB of `pytest-of-unknown/` the in-container suite runs had left in the worktree (pytest's basetemp falls back to the repo root because `$USER` is unset in the sandbox). Resume 3 took all of that as feedback, applied 7 of 8 items, reached `finish` and **passed `--verify` inside the sandbox** (the git-clean command); Claude removed one duplicate import. Said here and in the PR, per the owner's rule that Claude implements only after the worker fails a resume-with-feedback.

**A/B — Claude Sonnet subagent vs the released dirtywork + qwen, same approved design, same base (`ca0d926`):**

| implementer | wall | output at stop / finish | tests | review nits applied | cost | notes |
|---|---|---|---|---|---|---|
| Claude Sonnet subagent (Workflow, stopped at ~5 min on the owner's ruling) | ~5 min | +67/−2 across `toolspec.py` + `builtin_tools.py`; same `unit="seconds"` field and same description string; dict-driven unit→multiplier, "no unit means seconds" regex; richer comments | none yet (was to write them next) | n/a | API tokens | patch saved (`claude-partial-64.patch`); never ran the suite |
| dirtywork 0.10.1 + qwen3-coder-next (4 runs) | 18m51s of run time (≈50 min elapsed incl. three review gaps) | +256/−7 across 6 files: `toolspec.py`, `builtin_tools.py`, `test_toolspec.py`, `test_runner.py`, schema fixture regenerated, `.gitignore` | 26 (25 toolspec + 1 runner replay) — host suite 1359 → 1386 | 12 of 15 over three resumes; 3 finished by Claude | $0 (local) | found and regenerated the golden schema fixture unprompted; worked around S13 with `-k "not docker and not workspace"`; reached a green `--verify` inside the sandbox on run 4 once the gate unset `GIT_DIR`/`GIT_WORK_TREE` (S13) |

Same design, equivalent behaviour; qwen's helper is the simpler of the two (one regex, if/elif) and it shipped with tests. The cost of the local path was turns and review rounds, not correctness: ~1 of every 2 turns re-ran tests, the first three runs ended on `max_turns`, and one real defect (the unbounded `int()`) needed an adversarial pass to surface — which is what the review loop is for.

Sampler (5 s, both models resident throughout): free RAM min/median/max 0.06 / 3.05 / 4.59 GB during run 1 (n=56) and 0.06 / 5.64 / 12.72 GB across the three runs and the review gaps between them (19:54:20–20:15:37Z, n=246); the two 0.06 GB samples coincide with pytest running inside the 4 GB container (19:54:41, 19:57:59Z). **S12 (new polish candidate):** the model sends fractional floats for integer params — run 1's two `read_file … must be integer, got float` errors were `offset: 208.5` and `offset: 24.5` — decide whether to truncate/round or to name the accepted form in the error the way #64 now does for `timeout`.

**S13 (new, dogfood finding): the sandbox exports `GIT_DIR=/gitdir` and `GIT_WORK_TREE=/work` into every command, so a repo whose tests shell out to `git` in temp dirs cannot pass `--verify` inside the sandbox** — dirtywork's own suite falls apart there — resume 1's in-sandbox full run: `98 failed, 1154 passed, 27 deselected, 131 errors` (nearly all of tests/test_workspace.py errors out; the two signatures in the verify tail were `git init` landing in `/gitdir` → `FileNotFoundError …/.git/objects`, and `git worktree list` returning the parent repo's worktrees → `assert 215 == 1`); the worker had quietly worked around it with `-k "not docker and not workspace"`. Reproduced with a plain `docker run` on the pytest image (fails with the two vars exported, passes with `env -u GIT_DIR -u GIT_WORK_TREE`). Workaround used from resume 2 on: `--verify "env -u GIT_DIR -u GIT_WORK_TREE python3 -m pytest …"`. Candidate fix for 1.0: pass the two vars only to dirtywork's own git invocations, not to the model's commands / the gate (ties to #61). **Fixed by #61** (gitfile discovery; only the export container keeps the variables); the `env -u` workaround stays necessary only while the *released* sandbox running a dogfood build is older than that fix.

**PR #71 review (owner) → two more worker rounds, then Claude.** P1: the wire schema still said `"type": "integer"` while runtime accepted `"60s"`; P2: `canonical_args` did not normalize duration strings for a generic `unit="seconds"` param, so `"2m"` and `120` could dodge the stall detector. Sent as `resume --feedback` twice: resume 5 **called `finish` on turn 1 with no change** — its summary re-declared the original #64 work; the resumed history (27 turns, 6.8k prompt tokens since tool results are stored as previews) ended in `finish → run finished` and the model treated the review as already satisfied. **S14 (harness candidate, #66 family): reject a zero-change `finish` on the first turn of a feedback resume and re-deliver the feedback as a nudge.** Resume 6, with an explicit no-early-finish guard, did the work partially (helper + schema) and hit `max_turns` at 30 in 78 s (19 of 30 calls were `read_file`). Per the owner's rule, Claude finished P1/P2 after the second failed round: `_validate_args` routed through the same `_coerce_param`, fixture regenerated (`"type": ["integer", "string"]`), two tests, the contract sentence. Sampler for run 6: free RAM min/median/max 0.06 / 3.49 / 4.83 GB (n=18).

## #61 — sandbox strays: kill in place, gitfile discovery, reset notices (2026-08-25)

Built by the released dirtywork 0.10.1 (`pipx run --spec`) + qwen3-coder-next (LM Studio, 65k) in
`dirtywork-worker-pytest:0.10`, chained off `issue-61-sandbox-resets` with `--branch-from`; verify
gate `env -u GIT_DIR -u GIT_WORK_TREE python3 -m pytest -q -p no:cacheprovider` (the released
sandbox still exports `GIT_DIR`). Spec: `docs/superpowers/specs/2026-08-25-sandbox-strays-gitfile-and-reset-notices-design.md`;
plan: `docs/superpowers/plans/2026-08-25-issue-61-sandbox-strays.md`. Sampler: `metrics-61.csv`
(scratchpad; per-window stats at the end).

| run (slug) | task | status | wall | turns | s/turn | prompt tok | compl tok | tok/s | nudges | guardrail | resets | tool mix | verify |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `issue-61-task-w1-of-0825114114-a761a566` | W1 gitfile layout (run 1) | `max_turns` (work complete, no `finish`) | 220 s | 60 | 3.7 | 1,754,692 (≈29.2k/turn) | 12,697 | ≈8.0k | 0 | 0 | 0 | read_file 27 / edit_file 17 / bash 6 / list_dir 4 / append_file 2 / grep 2 / write_file 1 | not reached — host: 1388 passed, 2 failed (2 test-side bugs: `PATH=` prefix, `-e` index) |
| `issue-61-task-w1-of-0825114955-0806e529` (resume 1, Claude review: 6 items) | W1 gitfile layout | `completed` — `finish` reached, **verify PASSED in-sandbox** (1390) | 246 s | 37 | 6.7 | 718,541 | 5,407 | ≈2.9k | 0 | 0 | 0 | read_file 14 / bash 13 / edit_file 9 / finish 1 | passed; host 1389 passed / 1 skipped; 6/6 feedback items applied; Claude dropped whitespace-only edits to 2 unrelated files |
| `issue-61-task-w2a-of-0825115712-dfb0e788` | W2a export enumeration/roots (run 1) | `completed` — `finish` reached, **verify PASSED in-sandbox** (1396) | 555 s | 57 | 9.7 | 1,933,596 (≈33.9k/turn) | 18,941 | ≈3.5k | 0 | 0 | **1** (`stray process after bash` right after `grep -n "find" tests/test_export_flow.py`, exit 0 — the class-E watchdog race of spec §1.2, live in our own build) | read_file 31 / edit_file 17 / bash 8 / finish 1 | passed; host 1396 passed / 1 skipped; review: unterminated-tail rule inverted in code+test, 34 whitespace lines → resume |
| `issue-61-task-w2a-of-0825120900-6bbcd080` (resume 1, Claude review: 3 items) | W2a export enumeration/roots | `completed` on turn 2 — **zero-change finish** (S14 / #66 family): ran the suite, called `finish`, applied 0/3 items | 117 s | 2 | 58.6 | 13,842 | 198 | ≈0.1k | 0 | 0 | 0 | bash 1 / finish 1 | passed (nothing changed) → resume 2 with check-named feedback |
| `issue-61-task-w2a-of-0825121248-6cd0a8da` (resume 2, same 3 items with a shell check named per item) | W2a export enumeration/roots | `completed` — 3/3 items applied, **verify PASSED in-sandbox** (1396) | 127 s | 19 | 6.7 | 188,281 | 1,315 | ≈1.5k | 0 | 0 | 0 | bash 9 / read_file 5 / edit_file 3 / apply_edits 1 / finish 1 | passed; `git diff --check` clean; host 1396 / 1 skipped |
| `issue-61-task-w2b-of-0825121710-7fdb8208` | W2b export splice + safety net (run 1) | `max_turns` (splice + net written; 2 test-scripting failures being debugged, no `finish`) | 471 s | 60 | 7.8 | 2,213,624 (≈36.9k/turn) | 16,759 | ≈4.7k | 0 | 0 | 0 | read_file 28 / bash 23 / edit_file 9 | not reached; host 1401 passed, 2 failed (test scripting) — review: exec_argv needlessly widened, silent child-tree guard, duplicated record parsers, 70 whitespace lines → resume |
| `issue-61-task-w2b-of-0825122759-b6059873` (resume 1, Claude review: 7 items with checks) | W2b export splice + safety net | `max_turns`/45 — 3/7 applied (whitespace, child guard, partial rename); `docker_args` not reverted, parsers not merged; left `tests/test_export_flow.py` uncollectable (8 errors) | 226 s | 45 | 5.0 | 1,469,338 | 9,728 | ≈6.5k | 0 | 0 | 0 | read_file 19 / bash 11 / edit_file 8 / grep 6 / apply_edits 1 | not reached |
| — (Claude finish after the failed resume, per the dogfood rule) | W2b export splice + safety net | commit `b86745b` on top of the verbatim export `3597c78`: over-indented child read-tree block, `nested_tree` rename completed, one `parse_z_records` for ls-files/ls-tree, `exec_argv` widening reverted, `Captured` kwargs in one test, whitespace | — | — | — | — | — | — | — | — | — | — | host 1403 passed / 1 skipped; **live splice check PASS** (`p8-replay.py` on the branch runtime: 12/12 expected changes across 6 nested roots, no gitlink, `__pycache__` excluded via the top-level `.gitignore`); note: a macOS `sed 's/[ \t]*$//'` sweep on the host stripped trailing `t`s and was recovered from the run's `diff.patch` |
| `issue-61-task-w3-of-0825124011-bbf5cc47` | W3 strays.py module + callable fakes (run 1) | `completed` — `finish` reached, **verify PASSED in-sandbox** (1436) | 445 s | 55 | 8.1 | 1,591,305 (≈28.9k/turn) | 19,316 | ≈3.6k | 0 | 0 | 0 | read_file 15 / edit_file 13 / bash 13 / list_dir 5 / grep 5 / write_file 2 / insert_after 1 / finish 1 | passed; host 1436 / 1 skipped; review: scripts byte-exact; `parse_locks` unterminated-tail rule inverted, `cap_locks` `not truncated` inverted, `parse_tether_pid` strips one newline only, 10 whitespace lines → resume |
| `issue-61-task-w3-of-0825125017-516c446d` (resume 1, Claude review: 6 items with checks) | W3 strays.py | `completed` on turn 1 — **zero-change finish** (S14): called `finish` first, applied 0/6 | 61 s | 1 | 60.6 | 8,358 | 415 | ≈0.1k | 0 | 0 | 0 | finish 1 | passed (nothing changed) → resume 2 with the do-not-finish-until-checks-pass preamble |
| `issue-61-task-w3-of-0825125258-67a8c6a6` (resume 2, same items, do-not-finish preamble) | W3 strays.py | `max_turns`/45 — all 6 code items applied and their checks pass; 3 tests still encode the old behaviour; no `finish` | 146 s | 45 | 3.2 | 696,839 | 5,717 | ≈4.8k | 0 | 0 | 0 | bash 42 / read_file 2 / list_dir 1 | not reached; host 1433 passed / 3 failed (test-side) → Claude finishes the tests |
| — (Claude finish after two resumes, per the dogfood rule) | W3 strays.py | three assertions still encoded the inverted rules (tether pid stripped; `cap_locks` total only when not truncated) — fixed on top of the verbatim export | — | — | — | — | — | — | — | — | — | — | host 1436 passed / 1 skipped; `git diff --check` clean |
| `issue-61-task-w4a-of-0825125912-59fd4fd2` | W4a tether pid, notices, `reset(strays=)` (run 1) | `max_turns` (source complete and per brief; 5 test failures being debugged, no `finish`) | 420 s | 60 | 7.0 | 2,206,578 (≈36.8k/turn) | 15,364 | ≈5.3k | 0 | 0 | 0 | read_file 32 / edit_file 17 / grep 10 / bash 4 | not reached; host 1445 passed, 5 failed (test-side) → resume |
| `issue-61-task-w4a-of-0825130906-0e76bc66` (resume 1, Claude review: 5 items with checks) | W4a tether pid, notices, `reset(strays=)` | `completed` — 5/5 applied, **verify PASSED in-sandbox** (1450) | 174 s | 36 | 4.8 | 578,604 | 3,133 | ≈3.3k | 0 | 0 | 0 | bash 24 / read_file 6 / edit_file 5 / finish 1 | passed; host 1450 / 1 skipped; `git diff --check` clean; no Claude edits |
| `issue-61-task-w4b-of-0825131354-7ca72e00` | W4b stray ladder (run 1) | `max_turns` — ladder written in docker.py, **no tests**, 32 bash calls wandering in git; no `finish` | 218 s | 60 | 3.6 | 2,178,403 (≈36.3k/turn) | 11,710 | ≈10.0k | 0 | 0 | 0 | bash 32 / read_file 22 / edit_file 8 | not reached; host 1450 (unchanged); review: lock sweep unguarded (runs on every bash call), a comment block corrupted by the `_SETTLE_SLEEP` insertion, truncation note nested under rc≠0, 32 whitespace lines → resume |
| `issue-61-task-w4b-of-0825132026-3e14c990` (resume 1, Claude review: 3 code fixes + full test list) | W4b stray ladder | `completed` on turn 1 — **zero-change finish** (S14, third of this build) | 58 s | 1 | 58.3 | 9,172 | 155 | ≈0.2k | 0 | 0 | 0 | finish 1 | passed (nothing changed) → resume 2 with the do-not-finish preamble |
| `issue-61-task-w4b-of-0825132307-906ac2fe` (resume 2, do-not-finish preamble) | W4b stray ladder | `max_turns`/60 in 109 s — **wiped its own work**: a class-E reset (after `grep -n "def _kill_strays…"`, exit 0) re-initialized the index to the base commit, then the worker ran `git checkout dirtywork/sandbox/docker.py` (turn 64) and restored the BASE file — S11 in our own build; diff collapsed to +3 lines | 109 s | 60 | 1.8 | 1,506,572 | 5,039 | ≈13.9k | 0 | 0 | **1** (class E) | bash 37 / read_file 20 / edit_file 2 / apply_edits 1 | not reached → Claude finishes (run-1 ladder recovered from its `diff.patch`) |
| — (Claude finish after two failed resumes, per the dogfood rule) | W4b stray ladder | run-1 ladder restored from its `diff.patch`; `_sweep_locks`/`_reap` rewritten per §3.4 (sweep only after a successful kill, one `cap_strays`, OOM before the sweep, truncation note unnested), the corrupted `WRITE_SCRIPT` comment restored, `_SETTLE_SLEEP` placed; fixtures now script discovery/kill/sweep so the rung is active by default; ladder tests a–i written | — | — | — | — | — | — | — | — | — | — | host 1460 passed / 1 skipped; `git diff --check` clean |
| `issue-61-task-w5a-of-0825133234-fab76155` | W5a `_reap_lock`, `_sample_worktree(wait=)`, watchdog `wait=` (run 1) | `max_turns` — code + tests written; one threading test failing; no `finish` | 253 s | 60 | 4.2 | 2,064,156 (≈34.4k/turn) | 9,677 | ≈8.2k | 0 | 0 | 0 | read_file 30 / edit_file 15 / bash 14 / append_file 1 | not reached; host 1464 passed, 1 failed (worker's own deadlock test); 84 whitespace lines |
| `issue-61-task-w5a-of-0825133928-b5ba9d43` (resume 1, Claude review: 4 items with checks) | W5a reap lock + `wait=` sampling | `max_turns`/45 — deadlock test still failing, whitespace untouched; Claude's own item-1 check script was broken (`inspect.getsource` keeps the def indent), which likely cost turns | 135 s | 45 | 3.0 | 986,902 | 7,553 | ≈7.3k | 0 | 0 | 0 | bash 27 / read_file 14 / edit_file 4 | not reached; host 1465 / 1 failed → Claude finishes |
| — (Claude finish after the failed resume, per the dogfood rule) | W5a reap lock + `wait=` sampling | the worker's resume had applied the whole-ladder lock and the `None`-safe watchdog; Claude rewrote the deadlock test to assert what §3.6 guarantees (no deadlock; resets serialized under `_reset_lock`) and swept whitespace | — | — | — | — | — | — | — | — | — | — | host 1466 passed / 1 skipped; `git diff --check` clean |
| `issue-61-task-w5b-of-0825134617-463ac40e` | W5b flags, violation consumption, shutdown, `bash()` finally, abandoned grep (run 1) | `max_turns` — code written (+148 in docker.py), tests barely started (+24), 3 failures; no `finish` | 387 s | 60 | 6.4 | 2,109,745 (≈35.2k/turn) | 12,991 | ≈5.5k | 0 | 0 | 0 | read_file 34 / edit_file 14 / grep 7 / bash 5 | not reached; host 1463 passed, 3 failed → resume |
| `issue-61-task-w5b-of-0825135532-effa4abb` (resume 1, Claude review: 3 code fixes + tests a–g) | W5b flags/shutdown/abandoned exec | `max_turns`/60 — `bash()` fix and the abandoned-exec lock applied, 7 tests written; `_sample_worktree` re-read skipped; 5 tests failing (grep/bash timeout scripting) | 185 s | 60 | 3.1 | 1,709,407 | 9,514 | ≈9.2k | 0 | 0 | 0 | bash 33 / read_file 19 / grep 4 / edit_file 4 | not reached; host 1471 / 5 failed → Claude finishes |
| — (Claude finish after the failed resume, per the dogfood rule) | W5b flags/shutdown/abandoned exec | `_sample_worktree` violation re-reads added; four tests repaired (`docker_cli` import, `_run` stand-in signature, bash-timeout test now scripts only the bash exec and expects the in-place kill, reset-raises test fails fast through an init rc 1 instead of a 60 s ready-wait) | — | — | — | — | — | — | — | — | — | — | host 1476 passed / 1 skipped; `git diff --check` clean |
| `issue-61-task-w6-of-0825140609-518316c1` | W6 runner `drain_sandbox()` + five call sites (run 1) | `max_turns` — runner wiring done (6 `drain_sandbox()` occurrences), 434 test lines written, 5 tests failing; no `finish` | 449 s | 60 | 7.5 | 2,218,091 (≈37.0k/turn) | 15,400 | ≈4.9k | 0 | 0 | 0 | read_file 31 / bash 11 / edit_file 10 / grep 8 / append_file 1 | not reached; host 1480 passed, 5 failed; 130 whitespace lines |
| — (P7 race regression on the integration branch after W5b, `scratchpad/p7/probe.py` + `probe2.py`, `PYTHONPATH=.worktrees/issue-61-sandbox-resets`) | acceptance for §3.6 | **phase A 0/40 resets** (was 4/40 on 0.10.1), control 0/40, phase B 0/5, forced overlap (2 s sample, 1 s bash) **0/5** (was 5/5 with double resets) | ≈9 min | — | — | — | — | — | — | — | **0** | — | the watchdog's own sample is never reaped or killed |
| `issue-61-task-w6-of-0825141648-0982a8fa` (resume 1, Claude review: `_NoticeSandbox(HostSandbox)` double + 5 recipes) | W6 runner drain | `max_turns`/60 — double replaced, 3 of 5 scenarios fixed; 2 tests still failing (malformed variant, an extra empty-reply test); 70 whitespace lines | 402 s | 60 | 6.7 | 1,924,395 | 19,040 | ≈4.8k | 0 | 0 | 0 | bash 26 / read_file 19 / edit_file 15 | not reached; host 1482 / 2 failed → Claude finishes |
| — (Claude finish after the failed resume, per the dogfood rule) | W6 runner drain | two tests rebuilt: the mixed turn puts `_bad_entry()` inside one response beside the bash call (follow-up = malformed text + notice, kinds in the documented order); the text-only case queues a notice between turns (the watchdog-thread case) and asserts `NUDGES["empty"] + notice` via `user` | — | — | — | — | — | — | — | — | — | — | host 1484 passed / 1 skipped; `git diff --check` clean; runner.py untouched by Claude |
| `issue-61-task-w7-of-0825142803-e1610416` | W7 bench / runs show / soak / wire text / schema test (run 1) | `max_turns` — **zero edits**: 34 `read_file` + 18 `grep` + 7 `bash`, the model kept re-reading the task; no `finish` | 313 s | 60 | 5.2 | 2,225,833 (≈37.1k/turn) | 4,972 | ≈7.1k | 0 | 0 | 0 | read_file 34 / grep 18 / bash 7 / list_dir 1 | not reached; host 1484 (unchanged) → resume with a forcing preamble |
| `issue-61-task-w7-of-0825143512-0514bd22` (resume 1, forcing preamble) | W7 evidence surfaces | `max_turns`/60 — all five surfaces edited (bench, runs, soak, wire text + fixture regenerated), runs/schema tests written; bench/soak tests missing; no `finish` | 222 s | 60 | 3.7 | 2,219,947 | 8,694 | ≈10.0k | 0 | 0 | **1** (class E, see transcript) | read_file 27 / edit_file 17 / bash 15 / apply_edits 1 | not reached → Claude finishes |
| — (Claude finish after the failed resume, per the dogfood rule) | W7 evidence surfaces | schema tests matched against the backticked section headings; `_harness_failures` `timeouts` paragraph restored; joined code spans no longer cut at 120 chars in the Markdown callouts; `soak_harvest` now sets `stray_kills` (the worker read a key nothing wrote); wire text reflowed; bench + soak tests added | — | — | — | — | — | — | — | — | — | — | host 1497 passed / 1 skipped; `git diff --check` clean |
| `issue-61-task-w8a-of-0825144459-d432f168` | W8a live tests 13–16, 20a/b + image `ENV` (run 1) | `max_turns` — Dockerfile `ENV` + README line done, 6 live tests + a helper written (collect), mid-fix of a quote typo; no `finish` (the worker cannot run docker-marked tests) | 158 s | 60 | 2.6 | 1,723,227 (≈28.7k/turn) | 8,159 | ≈10.9k | 0 | 0 | 0 | bash 34 / edit_file 14 / read_file 12 / list_dir 2 | not reached; host 1497 unchanged; live suite run by Claude next |
| `issue-61-task-w8a-of-0825145112-a16bac21` (resume 1: the missing `_events`/`_of` import) | W8a live tests | `completed` — import added, **verify PASSED in-sandbox** (1497) | 65 s | 7 | 9.3 | 55,113 | 540 | ≈0.8k | 0 | 0 | 0 | bash 4 / read_file 1 / edit_file 1 / finish 1 | passed; Claude's host live run: **14 passed, 3 failed** (int-parse of a multi-line result; notice looked for in `result` instead of `follow_up`; 20a expects a reset from a fork bomb the fork-free kill survives) → resume 2 |
| `issue-61-task-w8a-of-0825145434-78b3e4c8` (resume 2: 3 test edits from the host live run) | W8a live tests | `completed` — 3/3 applied, **verify PASSED in-sandbox** (1497); Claude's host live run: **17 passed, 2 skipped** (`.NET` tests skip on the pytest image) | 109 s | 21 | 5.2 | 459,902 | 2,689 | ≈4.2k | 0 | 0 | 0 | read_file 12 / bash 5 / edit_file 3 | passed; no Claude edits on this task |
| — (Claude, host: `-m docker` on `dirtywork-worker-dev:issue61`, the branch Dockerfile with all five .NET variables; SDKs 8.0.424 + 10.0.400) | C9 step 1 evidence (after W8a) | **19 passed** (incl. the net8.0/net10.0 build+run tests), 1 deselected | 83 s | — | — | — | — | — | — | — | — | — | the pytest image: 17 passed / 2 skipped (no SDK) |
| `issue-61-task-w8b-of-0825145829-211e99d5` | W8b live tests 17–19, 21 (run 1) | `max_turns` — `_SlowClient` + `client_cls` plumbing and the three tests written; the file left uncompilable (backslash escapes mid-fix); one text-shaped tool call (`tail-10 …</parameter`, the #67 family); no `finish` | 243 s | 60 | 4.1 | 1,663,443 (≈27.7k/turn) | 14,864 | ≈6.8k | 0 | 0 | 0 | read_file 24 / edit_file 14 / grep 12 / bash 8 / apply_edits 1 / malformed 1 | not reached; collection error → resume |
| `run-the-test-suite-with-0825150424-1285672f` (C9 step 2 — the BRANCH runtime, `python3 -m dirtywork run` from the integration worktree) | **S13 acceptance**: plain gate `python3 -m pytest -q -p no:cacheprovider` inside the sandbox, no `env -u` | `completed` — worker's own run 1498 passed; **verify PASSED in-sandbox (1498)**; 0 `sandbox_reset`, 0 `stray_kill` | 107 s | 2 | 53.4 | 4,423 | — | — | 0 | 0 | 0 | bash 1 / finish 1 | passed — S13 closed: git inside the sandbox is discovered through the gitfile, tests that `git init` in temp dirs pass |
| `issue-61-task-w8b-of-0825150400-dc7425f1` (resume 1: escaping, duplicate test, SDK check, whitespace) | W8b live tests 17–19, 21 | `completed` — 4/4 applied, **verify PASSED in-sandbox** (1497); 22 docker tests collect | 216 s | 33 | 6.5 | 482,795 | 3,470 | ≈2.2k | 0 | 0 | 0 | bash 15 / read_file 11 / edit_file 6 / finish 1 | passed; host live runs (slow race loop; .NET on both images) follow |
| — (Claude, host: `test_docker_live_dotnet_build_leaves_no_stray` on `dirtywork-worker-dev:issue61` and on `dirtywork-worker-dev:issue63`) | C9 — the D3-issue97 `.NET` daemon half of S11 | **branch image: `dotnet new` + `dotnet build` leaves no stray, no kill, no reset; issue-63 image (no daemon variables): exactly one `stray_kill` naming `VBCSCompiler`, no reset** | 4.5 s + 5.0 s | — | — | — | — | — | — | — | 0 | — | the git-stash half of S11 is live test 13 (stash survives a stray kill), passing on both images |
| `bash-buildsh-should-write-outtxt-0825150526-2e11d323` (C9 step 3 — `tools/soak_driver.py`, provoker `sh-wait-for-service`, BRANCH runtime, `dirtywork-worker-pytest:0.10`) | soak class A/D family re-run | `completed`, **acceptance passed**; the model did not background anything this time → 0 `stray_kill`, 0 `sandbox_reset` | 177 s | 26 | 6.8 | 162,510 | 3,552 | ≈0.9k | 1 (`empty`) | 1 | 0 | bash 18 / other 8 | passed — a clean run-continues datapoint; the ladder itself is demonstrated by live tests 13–21 |
| — (Claude, host: full live suite with `DIRTYWORK_LIVE_SLOW=1` on the pytest image, W8b worktree) | W8b live tests | 16 passed / 2 skipped / **4 failed, all test-side**: race loop ended `stalled` (40 identical read-only calls trip the stall detector → `stall_turns=0`); pid-flood helper demanded `strays` on a `budget sample failed` reset; the .NET test ran the build on the pytest image (.NET 8 without the W^X variable dies, #70) instead of skipping; the grep test's 200 MB file made the export fail before the grep assertion (→ a FIFO `rg` blocks on) | 139 s | — | — | — | — | — | — | — | — | — | → resume 2 |
| `issue-61-task-w8b-of-0825151413-ee187f86` (resume 2: the four live-test edits) | W8b live tests | `completed` on turn 1 — **zero-change finish** (S14, fourth of this build) | 57 s | 1 | 56.6 | 7,029 | 193 | ≈0.1k | 0 | 0 | 0 | finish 1 | passed (nothing changed) → Claude finishes (two resumes spent) |
| — (Claude finish of W8b: the four live-test edits, then the full live suite with `DIRTYWORK_LIVE_SLOW=1` on the pytest image) | W8b live tests | **18 passed / 3 skipped / 1 failed**: the race loop now ends `max_turns` (40 bash turns + `finish` exceed the default cap) → `max_turns=60` added and the test rerun alone | 319 s | — | — | — | — | — | — | — | — | — | the `.NET` test now skips correctly on the pytest image (#70) |
| — (Claude, host: `test_docker_live_race_loop_no_resets`, pytest image, `DIRTYWORK_LIVE_SLOW=1`, after `stall_turns=0` + `max_turns=60`) | spec §9 test 17 — the P7 race as a regression test through the real runner | **1 passed**: 40 `sed` calls with 5.2 s idle between turns, **0 `sandbox_reset`, 0 `stray_kill`** (the released 0.10.1 reset 4/40 in the same loop) | 221 s | 41 | 5.4 | — | — | — | 0 | 0 | **0** | bash 40 / finish 1 | — |
| — (Claude finish after two resumes, per the dogfood rule) | W8b live tests | four test-side edits: `stall_turns=0` + `max_turns=60` on the race test; pid-flood helper requires `strays` only on stray-caused resets; `.NET` test skips when the build cannot run on the image (#70); grep test blocks `rg` on a FIFO instead of exporting a 200 MB file | — | — | — | — | — | — | — | — | — | — | host live on the pytest image: 19 passed / 3 skipped of 22 |

**#61 build metrics (sampler `tools/soak_sampler.sh`, 5 s cadence, per-window stats computed at the end):**

```
samples 2771  16:28:15→20:26:48 UTC  (5 s cadence)
window        n   free GB min/med/max   inactive GB min/max   models
16:00 UTC     369    0.19/ 3.27/11.98      21.58/50.14        2
17:00 UTC     697    0.03/ 6.60/16.32      19.53/49.14        2
18:00 UTC     696    0.04/ 7.53/17.07      19.32/49.36        2
19:00 UTC     697    0.14/ 6.98/16.63      18.98/49.93        2
20:00 UTC     312    2.63/11.23/14.04      22.72/44.68        2
overall free GB: min 0.03 | p5 1.97 | median 7.21 | max 17.07; samples with free < 1 GB: 40 (1.4%)
```

Run totals (29 `dirtywork run`/`resume` invocations of 0.10.1 + qwen3-coder-next, `~/.dirtywork/runs/issue-61-task-*`): 1,245 turns, 113 min of run time, 36.9M prompt tokens, 244k completion tokens, $0. 12 `completed` (verify passed in-sandbox), 17 `max_turns`. Claude finished 8 tasks after failed resumes (W2b, W3, W4b, W5a, W5b, W6, W7, W8b); 4 zero-change `finish`es on feedback resumes (S14); 1 zero-edit run (W7 run 1); 3 sandbox resets hit the build itself (2 class E after plain `grep`/`sed`, 1 that led the worker to wipe its own work — S11 live).
| — (Claude, host: final `-m docker` suite on `dirtywork-worker-dev:issue61` from the integration branch, all twelve tasks landed) | C9 step 1 | **21 passed / 1 skipped** (the slow race loop, run separately: 0/40) of 22; pytest image: 19 passed / 3 skipped | 111 s | — | — | — | — | — | — | — | — | — | — |

**PR #74 review (Jim, 16:01 CDT): one P1** — `_sample_worktree()`/`_reap()` checked `watchdog.violation` and then called `reset()` without re-checking under `_reset_lock`; a `_watchdog_kill` landing in that gap had its already-killed container restarted (spurious `sandbox_reset`; a failed restart would mask the budget error). Fixed by W9 below, dogfooded the same way (commit `83ee7f3`, worker export verbatim). Sampler during the run (21:05:40–21:09:30 UTC): 44 samples, free RAM 1.30–5.03 GB (mean 2.64), both models resident.

| `issue-61-review-follow-up-w9-0825160545-7236c81c` (after the PR #74 review) | **W9 — review P1**: `reset()` re-checks the watchdog under `_reset_lock` (returns bool; `_reap`'s five pre-checks and `_sample_worktree`'s shutdown pre-check become callers); three `_FiringLock` tests | `completed` — 3/3 code items + 4 test items applied byte-identically to the brief, **verify PASSED in-sandbox** (1501); host 1500 passed / 1 skipped; the three tests fail on the unfixed branch (the race test shows the spurious `kill`+`wait` pair) | 212 s | 46 | 4.6 | 983,055 (≈21.4k/turn) | 5,958 | ≈4.6k | 0 | 0 | **1** (`stray process after bash` after a `grep -c` — the class-E reset on 0.10.1, i.e. the P7 failure this branch removes, observed in the run that fixes its last review finding; the worker's work survived) | read_file 15 / bash 11 / edit_file 10 / grep 9 / finish 1 | passed (1 round) |

**Second review pass (Jim, 16:22 CDT, on 7803d78): one more P1** — a reset skipped for a pending violation left `_reset_this_call` unset, so `_after_bash` sampled the dying container and an over-cap sample overwrote the disk-floor reason at `watchdog.py` (`check_worktree_budget_once` assigned `violation` unconditionally; so did the other three breach paths — the same overwrite is reachable with no reset at all). Fixed by W10: first-wins in the watchdog + the skip marks the call. Both sandbox tests reproduce `top → sample → "worktree exceeds cap"` on 7803d78 before the fix. Sampler 21:26–21:34 UTC: 90 samples, free RAM 0.17–10.58 GB.

| `issue-61-review-follow-up-w10-0825162618-4a2e7a3a` (after the second review pass on 7803d78) | **W10 — review P1 #2**: first recorded violation wins (`Watchdog.record_violation`/`take_violation` under a leaf `_violation_lock`, all four breach paths; both consumers use `take`); `reset()` marks the call when it skips; five tests | `max_turns` — **work complete and correct** (code byte-identical to the brief, the W9 assertion correctly flipped to `is True`, sandbox tests byte-identical), no `finish`; turns went to the brief's own sloppy grep check (`.` unescaped matched `watchdog_violation =`, Claude's slip) and a stray `CHANGES_SUMMARY.md` | 228 s | 60 | 3.8 | 1,900,643 (≈31.7k/turn) | 9,306 | ≈8.3k | 0 | 0 | 0 | bash 22 / read_file 19 / edit_file 13 / grep 6 | — (no finish) |
| `issue-61-review-follow-up-w10-0825163155-4d0c3387` (resume 1: delete the stray file, run the suite, finish) | W10 | `completed` — **finish reached, verify PASSED in-sandbox** (1506); host 1505 passed / 1 skipped; but feedback item 1 was **not applied** (the worker re-ran its grep checks and finished; `CHANGES_SUMMARY.md` still in the export — untracked, removed on the host by Claude, never in the commit): S14 family, partial feedback + finish | 75 s | 9 | 8.3 | 85,352 | 1,078 | ≈1.1k | 0 | 0 | 0 | bash 6 / read_file 2 / finish 1 | passed (1 round) |

## #65/#66 — cap-aware truncation, truncation budget, change guard (2026-08-25)

Built by the released dirtywork 0.10.1 (`pipx run --spec`) + qwen3-coder-next (LM Studio, 65k) in
`dirtywork-worker-pytest:0.10`, chained off `issue-65-66-change-guard` with `--branch-from`; verify
gate `env -u GIT_DIR -u GIT_WORK_TREE python3 -m pytest -q -p no:cacheprovider` (the released
sandbox still exports `GIT_DIR`). Spec: `docs/superpowers/specs/2026-08-25-cap-aware-truncation-and-change-guard-design.md`
(v4); plan: `docs/superpowers/plans/2026-08-25-issue-65-66-change-guard.md`. Baseline on the branch
(3e92fd7 + spec/plan/schema docs): **1505 passed, 1 skipped, 37 deselected**. Sampler:
`metrics-6566.csv` (scratchpad; per-window stats at the end).

**S14 A/B (feedback resumes under the released runtime, which has no guard):** first completion
zero-change — counted as they happen: **1** — W1b resume 1 (`5a48930d`): yes, `finish` on turn 1 with a summary re-declaring run 1; the released runtime recorded it `completed`.

| run (slug) | task | status | wall | turns | s/turn | prompt tok | compl tok | tok/s | nudges | guardrail | resets | tool mix | verify |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `issue-65-cap-aware-truncation-task-0825191220-0681dfcf` | W1a-1 numbers/texts/counter (run 1) | `max_turns` — runner.py items 1–8 exact, __main__/schema-list/test_main/soak_tools edits done, but `note_truncation` written with JSON-escaped quotes (`\"` → SyntaxError, the #61 W8b shape) and none of the seven test_runner pins rewritten (39 `read_file`) | 328 s | 60 | 5.5 | 2,262,743 (≈37.7k/turn) | 8,473 | ≈6.9k | 0 | 0 | 0 | read_file 39 / edit_file 15 / apply_edits 3 / bash 3 | not reached — host: SyntaxError → resume 1 with a `sed` fix + `_trunc_dict(tc)` helper per pin |
| `issue-65-cap-aware-truncation-task-0825191955-4cba1848` (resume 1: `sed` fix + `_trunc_dict(tc)` helper, 7 pins) | W1a-1 | `max_turns`/40 — runner.py compiles (0 escapes); helper + pins (a)–(d) done; 5 one-line pins left (496, 528, 569, 667, 925) | 163 s | 40 | 4.1 | 776,255 | 10,024 | ≈4.8k | 0 | 0 | 0 | edit_file 14 / read_file 12 / bash 12 / insert_after 2 | not reached → resume 2 with the five lines |
| `issue-65-cap-aware-truncation-task-0825192340-9c94b31d` (resume 2: five one-line pins) | W1a-1 | `max_turns`/30 — reached `finish` and verify (1 round(s), failed: resume 1 had overwritten `FakeProvider`'s header while inserting the helper, 138 failures); the worker re-typed `FakeProvider` from memory (no `name`, no `context_window=`, PEP 604 annotations) and left pins e/g and the `extra` dict → **Claude finish**: `FakeProvider` header restored verbatim from HEAD, three one-liners | 115 s | 30 | 3.8 | 513,516 | 2,746 | ≈4.5k | 0 | 0 | 0 | bash 10 / read_file 10 / edit_file 5 / grep 3 / finish 1 / list_dir 1 | verify failed in-sandbox (FakeProvider) → Claude finish, host suite pending |
| `issue-65-task-w1a-2-of-0825192812-d115d401` | W1a-2 five #65 tests (run 1) | `max_turns`/60 — all five tests written (+294 lines), 177/179 pass; two test bugs (compared two different tools' messages verbatim; `run_end["extra"]["truncations"]` instead of the top-level field) | 461 s | 60 | 7.7 | 1,817,391 | 19,538 | ≈4.0k | 0 | 0 | 0 | read_file 28 / bash 18 / edit_file 9 / grep 3 / run 1 / append_file 1 | not reached → resume 1 with the two exact replacements |
| `issue-65-task-w1a-2-of-0825193638-40a899c8` (resume 1: two exact replacements) | W1a-2 | `max_turns`/30 — both replacements applied, no `finish` | 136 s | 30 | 4.5 | 494,868 | 3,746 | ≈3.7k | 0 | 0 | 0 | bash 19 / read_file 7 / edit_file 4 | not reached — host suite run by Claude |
| `issue-65-task-w1b-of-0825194046-25a1eb7c` | W1b abort + bench + harvest (run 1) | `completed`, verify PASSED in-sandbox (1 round) — the abort (both paths) and `_abort_kind` right; harvest regex re-shaped with a `[^ ]+` path constraint (breaks on a path with a space); **none of the three tests written** — finished with the task half done | 261 s | 57 | 4.6 | 1,480,528 | 6,973 | ≈5.7k | 0 | 0 | 0 | read_file 23 / bash 11 / grep 10 / edit_file 8 / list_dir 3 / glob 1 / finish 1 | passed (existing suite) → resume 1: exact regex, docstring sentence, the three tests |
| `issue-65-task-w1b-of-0825194610-5a48930d` (resume 1: exact regex, docstring, three tests) | W1b | `completed` on turn 1 — **zero-change finish (S14, first of this build)**: summary re-declared the run-1 work, verify passed on the existing suite, 0/3 items applied | 59 s | 1 | 58.9 | 7,940 | 223 | ≈0.1k | 0 | 0 | 0 |  | passed (nothing changed) → resume 2 with the do-not-finish preamble |
| `issue-65-task-w1b-of-0825194741-8c56eab9` (resume 2: do-not-finish preamble) | W1b | `max_turns`/40 — only the regex re-shaped again (`'[^']*'` quoted paths); no docstring, none of the three tests → **Claude finish** (exact regex, docstring sentence, three runner tests, bench assertion, harvest F5 test) | 113 s | 40 | 2.8 | 508,011 | 7,396 | ≈4.6k | 0 | 0 | 0 | bash 36 / read_file 2 / list_dir 1 / edit_file 1 | not reached → Claude finish |
| — (Claude finish after two failed resumes, per the dogfood rule) | W1b | `_TRUNCATED_CALL_RESULT_RE` set to the spec pattern (the worker had left a literal `` byte where `` belonged — a third JSON-escape casualty); `_abort_kind` docstring sentence; `test_six_cutoff_replies_end_the_run`, `test_consecutive_rule_wins_over_the_cutoff_budget`, `test_sixth_truncation_on_the_tool_path_records_its_result`; the bench assertion; `test_f5_fires_on_the_new_generic_truncation_wording`; host 1509 passed / 1 skipped on the run base (1505 + 4) | | | | | | | | | | | |
| `issue-66-task-w2a-of-0825200952-b3f47c2d` | W2a `parse_exit_code` move, `parse_change_head`/`net_change`, `ProgressTracker` rule | `completed`, verify PASSED in-sandbox (1 round) — every item per the brief, code verbatim; three tests with every listed case; nit: `parse_exit_code` appended at the end of tools.py rather than beside `is_timeout_result` | 319 s | 54 | 5.9 | 1,497,185 | 9,514 | ≈4.7k | 0 | 0 | 0 | read_file 22 / bash 18 / edit_file 10 / grep 3 | passed → host suite pending, land verbatim |
| `issue-66-task-w2b-1-of-0825201706-54376ee3` | W2b-1 `changes.py` (script, parser, texts), `FingerprintSandbox`, `git_parts`, tests 10–11 | `completed`, verify PASSED in-sandbox (1 round) — `FINGERPRINT_SCRIPT` byte-exact vs the spec (the brief's self-check `diff` printed SCRIPT-OK), no escaped quotes this time; six tests in `tests/test_changes.py` | 275 s | 54 | 5.1 | 1,555,000 | 9,528 | ≈5.7k | 0 | 0 | 0 | read_file 21 / bash 11 / edit_file 8 / list_dir 5 / grep 5 / write_file 2 / run_shell_command 1 / finish 1 | passed → host suite pending |
| `issue-66-task-w2b-2-of-0825202336-fa4dbb4a` | W2b-2 real-script host test (nested/unborn/non-ASCII) | `max_turns`/60 — the seven-check test written (+173 lines), 6/7 checks pass on the host: check 5 compared the post-rewrite fingerprint with the one from before step 4 (`fp2`, not `fp3`); a scratch duplicate `tests/test_changes_new.py` left behind | 270 s | 60 | 4.5 | 1,647,730 | 16,159 | ≈6.2k | 0 | 0 | 0 | bash 39 / read_file 12 / list_dir 6 / write_file 2 / append_file 1 | not reached → resume 1 (two edits) |
| `issue-66-task-w2b-2-of-0825202920-4893dafd` (resume 1: `fp3` comparison, delete the scratch file) | W2b-2 | `completed`, verify PASSED in-sandbox (1 round) — both edits applied | 122 s | 12 | 10.2 | 113,862 | 888 | ≈0.9k | 0 | 0 | 0 | bash 6 / read_file 4 / edit_file 1 / finish 1 | passed → host suite pending |
| `issue-66-task-w3a-1-of-0825203325-0a16533a` | W3a-1 the guard in the runner (run 1) | `max_turns`/60 — runner.py items 1–8 per the brief (+91 lines, compiles), schema lists and two pins done; the finish-time block placed AFTER `drain_sandbox()`/`flush()` (must be first); the raising doubles, the test_main two-turn consequence and tests 12–14 not done — host: 5 failures, all of those | 424 s | 60 | 7.1 | 2,307,838 | 8,560 | ≈5.5k | 0 | 0 | 0 | read_file 39 / edit_file 12 / grep 9 | not reached → resume 1 (four items) |
| `issue-66-task-w3a-1-of-0825204323-35d77fa0` (resume 1: four items) | W3a-1 | `max_turns`/40 — item 2 done (five doubles guard the fingerprint command); the block move, the test_main two-turn edits and tests 12–14 not done | 145 s | 40 | 3.6 | 968,846 | 7,469 | ≈6.7k | 0 | 0 | 0 | read_file 28 / grep 6 / edit_file 5 / bash 1 | not reached → resume 2 with the do-not-finish preamble |
| `issue-66-task-w3a-1-of-0825204623-b7ae4780` (resume 2: do-not-finish preamble; items 1, 3, 4) | W3a-1 | `completed`, verify PASSED in-sandbox (round 2) — item 3 done (test_main two-turn edits + `_resume_responses` write_file); the finish-time block still after `drain_sandbox()`; tests 12–14 not written → **Claude finish** | 311 s | 40 | 7.8 | 958,159 | 9,190 | ≈3.1k | 0 | 0 | 0 | read_file 21 / edit_file 12 / grep 3 / finish 2 / bash 2 | passed (existing suite) → Claude finish (block move + tests 12–14) |
| — (Claude finish after two failed resumes, per the dogfood rule) | W3a-1 | the finish-time fingerprint block moved to the first statement of `finish()` (before `drain_sandbox()`); tests 12–14 (`test_start_fingerprint_before_first_chat`, `test_start_fingerprint_failure_turns_guard_off`, `test_zero_change_finish_rejected_once_then_completed`, `test_zero_change_finish_ends_unchanged_when_required` with its two variants); host 1528 passed / 1 skipped on the run base (1524 + 4) | | | | | | | | | | | |
| `issue-66-task-w3a-2-of-0825210031-8556490b` | W3a-2 tests 15–17 + 19 (run 1) | `max_turns`/60 — all four tests written (+491 lines); 3 fail + 1 error on test-side mistakes (`FingerprintSandbox` taken as a fixture parameter, a response script one turn short, a wrong expected string, one assertion on the wrong variable) | 365 s | 60 | 6.1 | 2,146,144 | 9,034 | ≈5.9k | 0 | 0 | 0 | read_file 29 / bash 26 / edit_file 2 / grep 1 / list_dir 1 / tail -100 tests/test_runner.py
</parameter 1 | not reached → resume 1 (four exact fixes) |
| `issue-66-task-w3a-2-of-0825210804-39fb0016` (resume 1: four exact test fixes) | W3a-2 | `verify_failed` after 3 rounds in 8 turns — called `finish` three times without applying the edits (the same assertions failed each round) | 342 s | 8 | 42.7 | 93,367 | 13,129 | ≈0.3k | 0 | 0 | 0 | read_file 3 / bash 2 | failed ×3 → resume 2 with the do-not-finish preamble |
| `issue-66-task-w3a-2-of-0825211444-1ca5c443` (resume 2: do-not-finish preamble) | W3a-2 | `max_turns`/40, one failed verify round — the same 3 failures + 1 error remain → **Claude finish** (the four test fixes) | 228 s | 40 | 5.7 | 1,088,447 | 10,010 | ≈4.8k | 0 | 0 | 0 | read_file 20 / grep 9 / bash 7 / edit_file 2 / brep 1 | failed → Claude finish |
| — (Claude finish after two failed resumes, per the dogfood rule) | W3a-2 | the four tests rewritten to the spec (own transcript per sub-case, the mixed turn with a timed-out bash call, `FingerprintSandbox` as a class); **found a runner defect from W3a-1**: the start fingerprint block sat *before* the loop`s `try:`, so a Ctrl-C during that exec escaped `run()` with no `run_end` (spec §4.1 (1)); moved inside the `try` in this finish and pinned by sub-case (d) (`interrupted`, turns 0, one `run_end`); host 1532 passed / 1 skipped on the run base (1528 + 4) | | | | | | | | | | | |
| `issue-66-task-w3b-of-0825213911-4ac56cc8` | W3b every-K `no_change` check (run 1) | `max_turns`/60 — `check_no_change` + `no_change_turns` + both call sites written, all four tests written (+300 lines); 195/196 pass: one response script one turn short | 794 s | 60 | 13.2 | 2,493,932 | 21,000 | ≈3.2k | 0 | 0 | 0 | read_file 28 / bash 18 / edit_file 14 / grep 4 | not reached → resume 1 (one fix) |
| `issue-66-task-w3b-of-0825215359-40c67621` (resume 1: sub-case (a) rewrite + the sub-case rules) | W3b | `max_turns`/40 — `test_no_change_check_skips` still fails on a response script that runs dry (a later sub-case) → **Claude finish** after one resume: the W3a-2 pattern (0 for 2 on multi-sub-case test scripts) made a second resume a poor bet | 311 s | 40 | 7.8 | 1,252,548 | 18,941 | ≈4.1k | 0 | 0 | 0 | bash 19 / read_file 13 / edit_file 5 / apply_edits 1 / write_file 1 / append_file 1 | not reached → Claude finish |
| — (Claude finish after one failed resume, stated above) | W3b | `test_no_change_check_skips` rewritten: (b) the finish on a K turn (rejected via the completion check, no K measurement), (c) `no_change_turns=0` still measures at `finish()`, (d) a guard-off run via a `None` start entry, (e) `BudgetExceeded`/`SandboxError` popped by a K check, (f) stall + no_change on one turn with the exact joined text and the `["empty", "stall", "no_change"]` order; host 1536 passed / 1 skipped on the run base (1532 + 4) | | | | | | | | | | | |
| `issue-66-task-w3c-of-0825221205-7b052db8` | W3c CLI wiring, resume block, `unchanged` gate, e2e tests (run 1) | `max_turns`/60 — every seam present (`_seed_payload`/`_contract_fields`, `require_changes=ctx.feedback is not None`, the stderr line, the gate message, the reordered block), test_resume pins rewritten, +190 lines of test_main tests; host suite pending | 176 s | 60 | 2.9 | 1,667,744 | 9,227 | ≈9.5k | 0 | 0 | 0 | read_file 38 / edit_file 13 / grep 7 / apply_edits 1 / bash 1 | not reached → host suite decides |
| `issue-66-task-w3c-of-0825221616-e2ab632b` (resume 1: rebuild the hand-built tool-call bodies with `tool_call_body`) | W3c | `max_turns`/40 — the SyntaxError is still there (bracket mismatch, now at line 1650) → **Claude finish** after one resume (a mechanical fix the worker did not land) | 87 s | 40 | 2.2 | 895,857 | 4,126 | ≈10.4k | 0 | 0 | 0 | bash 21 / read_file 18 / grep 1 | not reached → Claude finish |
| — (Claude finish after one failed resume) | W3c | the five CLI tests rewritten with the file`s body helpers (`test_fresh_run_zero_change_completes_with_changed_false`, `test_resume_feedback_zero_change_ends_unchanged`, `test_resume_feedback_with_a_write_completes_changed_true`, `test_resume_of_an_unchanged_run_requires_feedback`, `test_guard_off_prints_one_stderr_line`); host 1541 passed / 1 skipped on the run base (1536 + 5) | | | | | | | | | | | |
| `create-a-file-named-c1priortxt-0825222728-24665164` (**C1 prior**, branch runtime, host) | PASS | `completed`, `changed: true` — a real write; the guard measured a change on the first run under the branch runtime | 3 s | 2 | 1.7 | 4,355 | 70 | ≈1.3k | 0 (—) | 0 | 0 | write_file 1 / finish 1 | prior |
| `call-finish-immediately-with-the-0825222800-e17323b4` (**C1 case (c)**, branch runtime, host) | **PASS** | `completed`, `changed: false` — first `finish` refused with the plain-mode text as its own result (`nudge{unchanged_finish}` via `tool_result`, turn 1); second `finish` → `run finished`, `completed`, exit 0 | 3 s | 2 | 1.6 | 4,357 | 71 | ≈1.4k | 1 (unchanged_finish) | 0 | 0 | finish 2 | case (c) |
| `create-a-file-named-c1priortxt-0825222759-87538eaf` (**C1 case (a), attempt 1**, branch runtime, host) | refusal PASS, `unchanged` not reached | `max_turns`, `changed: false` — turn-4 `finish` refused with the required-mode text; the model wrote "The system rejected my finish because no changes were made. Let me reconsider" and used its last two turns thinking — `--max-turns 6` was too tight for a second completion → rerun with 14 | 8 s | 6 | 1.4 | 16,160 | 316 | ≈2.0k | 1 (unchanged_finish) | 0 | 0 | bash 4 / read_file 1 / finish 1 | case (a), attempt 1 |
| `create-a-file-named-c1priortxt-0825222844-565237a1` (**C1 case (a), attempt 2 (`--max-turns 14`)**, branch runtime, host) | refusal + K-nudge PASS, `unchanged` not taken | `max_turns`, `changed: false` — turn-3 `finish` refused with the required-mode text; the model wrote "perhaps I need to make a change and then revert it", then eleven bash turns grepping the tests for C1_PRIOR (no second `finish`); `nudge{no_change}` fired at turn 10 as designed. Two confounders: the scripted feedback ("No change is needed; call finish.") contradicts the required-mode text, and the text states the `unchanged` exit as a threat → attempt 3 with realistic already-satisfied feedback | 57 s | 14 | 4.1 | 66,036 | 835 | ≈1.2k | 2 (unchanged_finish/no_change) | 0 | 0 | bash 13 / finish 1 | case (a), attempt 2 (`--max-turns 14`) |
| `read-these-files-fully-with-0825222846-dfd0d4a3` (**C1 case (d)**, branch runtime, host) | **PASS** (fresh-run half) | `completed`, `changed: false` — 22 `read_file` calls in six turns, turn-6 `finish` refused with the plain-mode text, second → `run finished`; finished before turn 10 so the fresh-run `no_change` nudge was not reached here (it fired in (a) attempt 2) → (d2) with 44 files | 79 s | 7 | 11.2 | 236,405 | 2,412 | ≈3.0k | 1 (unchanged_finish) | 0 | 0 | read_file 22 / finish 2 | case (d) |
| `create-a-file-named-c1priortxt-0825222917-ba76cd59` (**C1 prior**, branch runtime, docker) | PASS | `completed`, `changed: true` — one `write_file`, `finish`; the guard measured the change inside the worker container | 18 s | 2 | 9.1 | 4,183 | 69 | ≈0.2k | 0 (—) | 0 | 0 | write_file 1 / finish 1 | prior |
| `create-a-file-named-c1priortxt-0825223332-405c7763` (**C1 case (a), attempt 3**, branch runtime, host) | **PASS** | `unchanged`, `changed: false` — feedback "Make sure C1_PRIOR.txt contains exactly one line, 'prior', with a trailing newline (fix it if not), then call finish." — already satisfied; turn-1 `finish` refused with the required-mode text; the model re-examined (hexdump, git status) and rewrote the file byte-identically with `write_file` — the fingerprint did not move; second `finish` → `run not finished: nothing changed`, **status `unchanged`, exit 1** | 15 s | 10 | 1.5 | 28,231 | 570 | ≈1.9k | 1 (unchanged_finish) | 0 | 0 | bash 6 / finish 2 / read_file 1 / write_file 1 | case (a), attempt 3 |
| (**C1 case (a2)**, branch runtime, host) | **PASS** | `dirtywork resume create-a-file-named-c1priortxt-0825223332-405c7763` without `--feedback` → exit 2, `error: run '…' ended 'unchanged' (the worker changed nothing); pass --feedback to tell it what to change` | — | 0 | — | 0 | 0 | — | 0 | 0 | 0 | — | (a2) |
| `create-a-file-named-c1priortxt-0825223429-67f9df59` (**C1 case (b)**, branch runtime, host) | **PASS** | `completed`, `changed: true` — the `unchanged` run resumed with a real item (append a line) → `append_file`, `completed`, exit 0; the file reads `prior`/`reviewed`. Whole S14 loop on one chain: prior → (a3) `unchanged` → (a2) gate → (b) `completed` | 6 s | 4 | 1.4 | 12,244 | 177 | ≈2.2k | 0 (—) | 0 | 0 | read_file 1 / append_file 1 / bash 1 / finish 1 | case (b) |
| `call-finish-immediately-with-the-0825223339-11ec448e` (**C1 case (c)**, branch runtime, docker) | **PASS** | `completed`, `changed: false` — first `finish` refused with the plain-mode text, second → `run finished`, `completed`, exit 0; fingerprint taken inside the worker container | 8 s | 2 | 4.0 | 4,201 | 71 | ≈0.5k | 1 (unchanged_finish) | 0 | 0 | finish 2 | case (c) |
| `create-a-file-named-c1priortxt-0825223432-d7f564a0` (**C1 case (a)**, branch runtime, docker) | **PASS — and the guard caught a false compliance claim** | `completed`, `changed: true` — the docker prior had written `prior` with no trailing newline; the same already-satisfied feedback as (a3) was therefore *not* satisfied here. The model's first `finish` claimed "5 bytes, which matches 'prior\n'" (false) → refused with the required-mode text → it re-examined, found the missing newline, rewrote the file (6 bytes) → second `finish` accepted, exit 0. The S14 shape (a summary that claims the feedback is applied when nothing changed), refused and then actually fixed | 10 s | 8 | 1.3 | 21,047 | 380 | ≈2.0k | 1 (unchanged_finish) | 0 | 0 | bash 4 / finish 2 / read_file 1 / write_file 1 | (a) docker |
| `create-a-file-named-c1priortxt-0825223523-5fbc833b` (**C1 case (b)**, branch runtime, docker) | **PASS** | `completed`, `changed: true` — the docker (a) run resumed with the real item → the file reads `prior`/`reviewed`, exit 0 | 17 s | 3 | 5.8 | 8,407 | 112 | ≈0.5k | 0 (—) | 0 | 0 | read_file 1 / append_file 1 / finish 1 | (b) docker |
| `read-these-48-files-fully-0825223443-1c6192ad` (**C1 case (d2)**, branch runtime, host) | PASS (fresh-run half) | `completed`, `changed: false` — 48 `read_file` calls in two turns (~24 per turn), turn-3 `finish` refused with the plain-mode text, second → `run finished`. No fresh run crossed turn 10, so the plain-mode `no_change` nudge is observed only in the unit suite; the required-mode variant fired live in (a) attempt 2 | 128 s | 3 | 42.5 | 116,083 | 2,694 | ≈0.9k | 1 (unchanged_finish) | 0 | 0 | read_file 48 / finish 2 | (d2) |

**C1 verdict: PASS.** Host: prior, (a3) `unchanged`, (a2) exit 2, (b) `completed`/`changed: true`, (c), (d), (d2). Docker: prior, (a) — the false compliance claim caught and then fixed, (b), (c). 13 runs, ~6 min wall. No `change guard off` stderr line in any run (the fingerprint measured every time, host and container). Observation (e): the guard's cost is invisible at this scale (fingerprint ≈60–80 ms on the host). Gate open → W4a launched at 104b3b0.
| `issue-6566-task-w4a-of-0825223625-4ec9e7fb` (**W4a**, fresh run, released 0.10.1) | diff complete, two test assertions off-brief | `max_turns` — all four brief items landed in 12 edits (`_STATUS_FAILURES`, `S14`, `MD_RESULT_FIELDS`, 6 new tests; touched suites 227 pass, compile clean) but no `finish` and no verify in 60 turns; the ten-wide nudges cell is still a substring check and `assert "unchanged" in out` is vacuous (the legend contains `unchanged_finish`) → feedback resume 1 | 210 s | 60 | 3.5 | 1,855,350 | 11,146 | ≈8.9k | 0 (—) | 0 | 0 | read_file 32 / edit_file 12 / grep 9 / bash 7 | W4a |
| `issue-6566-task-w4a-of-0825224226-cd829932` (**W4a feedback resume 1**, released 0.10.1) | **zero-change finish (S14 #2 of this build under the released runtime)** | `completed`, turn 1, one `finish`, no edit — the worktree diff is byte-identical to run 1's; the feedback (two assertion fixes + full suite) untouched. The branch runtime refused exactly this in C1 (a3) and docker (a). → Claude finish | 60 s | 1 | 60.5 | 6,741 | 160 | ≈0.1k | 0 (—) | 0 | 0 | finish 1 | W4a resume 1 |
| (**W4a Claude finish**, after one failed feedback resume) | landed `ad8c96d` + `4db1d44` | worker export committed verbatim; my commit changes only tests/test_bench.py::test_summarize_legend_and_detail_cell_list_ten_kinds — the s1 detail row split into cells, `cells[DETAIL_COLUMNS.index("nudges")] == "0/0/0/0/0/0/3/0/0/0"` and `cells[…("failures")] == "unchanged"` (exact, `len(cells) == len(DETAIL_COLUMNS)`), row status set to `unchanged`. Full suite on the run worktree: **1546 passed, 1 skipped** | — | — | — | — | — | — | — | — | — | — | W4a finish |
| `issue-66-task-w4b-of-0825224632-c57159aa` (**W4b**, fresh run, released 0.10.1) | brief followed exactly; the brief's premise was wrong | `completed` — one `append_file` of test 24 as briefed, non-docker suite run, `finish` with "the live run is the reviewer's". Live: **FAILED** on the default image — host fingerprint 4 lines, container 2: the docker seed's `tar --exclude=./.git` drops every `.git`, so the nested repositories made on the host arrive flattened (probe: `find . -name .git -mindepth 2` empty in the container). Spec §7 test 24's premise, not the worker's or the guard's → **#75** filed; Claude finish | 264 s | 38 | 6.9 | 825,036 | 3,883 | ≈3.1k | 0 (—) | 0 | 0 | read_file 17 / bash 14 / grep 5 / append_file 1 / finish 1 | W4b |
| (**W4b Claude finish**, spec premise correction) | landed `4ac1579` + `0e40d69` | worker export committed verbatim; my commit rewrites test 24: asserts the host fingerprint has four lines, that the seeded container has no nested `.git` and a two-line fingerprint, then creates the same nested repositories inside the container (`git init`/`add`/`commit`, and an unborn one) and asserts `fp_docker == fp_host` — content-addressed, sorted — then the store-growth and byte-identical-rewrite assertions as specified. Live: **PASS on both images** (default 3.2 s, `dirtywork-worker-pytest:0.10` 3.1 s) | — | — | — | — | — | — | — | — | — | — | W4b finish |
| (**C5 live suite, first pass**, integration tree 284daf0, pytest image) | **12 failed / 9 passed / 2 skipped** — a plan miss | every failure `model_error: IndexError('pop from empty list')`: the live tests script a completion (`_resp(content="done")` or a `finish` call) that changes nothing; the guard refuses a fresh run's first zero-change completion and asks again, and `ScriptedClient.reply` had nothing left. The spec's closure pass caught the `test_main` fakes (W3) but not the live scripts — not a runner defect (a real provider does not pop a list) | — | — | — | — | — | — | — | — | — | — | C5 live |
| (**C5 Claude fix**, one place) | `ScriptedClient` re-serves its last reply when the script is exhausted *and* that reply was a completion (plain answer or `finish`); any other exhaustion raises `IndexError('scripted responses exhausted before the run ended')` — loud, as before | the twelve rerun on the pytest image: **11 passed / 3 skipped / 0 failed** (334 s); full live suite rerun follows as the C5 record | — | — | — | — | — | — | — | — | — | — | C5 live |
| (**C5 live suite, final**, integration tree f44a68e, `dirtywork-worker-pytest:0.10`, all files) | **26 passed / 3 skipped / 0 failed** (396 s) | the three skips are the .NET-image conditions (#70); test 24 among the passes | — | — | — | — | — | — | — | — | — | — | C5 live |
| **F5 reruns (spec §8)** — scored with `$SCRATCH/f5_score.py`: recovery = `completed` + `truncations ≥ 1` + 401 rows in the export; `acceptance_passed` beside it. **Confound stated:** rows 5–6 (qwen 4096) and the Devstral row ran with a second driver and the live suite sharing the GPU, so their wall times are not comparable with the serial #64 baseline; the turn-4 strike aborts are load-independent | | | | | | | | | | | | | F5 |
| `create-fixturesrowscsv-with-a-header-0825224755-3b3377bc` (**F5 F5-1024-qwen-r1**, branch runtime, docker) | recovery | `completed` — Created fixtures/rows.csv with header row and exactly 400 data rows as per README.md schema. File structure verified: 401 total lines, proper LF line endings (no CR), ends with single trailing newline. Data rows have correct id (1-400), name (UserN), email (userN@example.com), plan (cycling free/pro/enterprise based on id%3), created_at (ISO-8601 dates from 2024-01-01 to 2025-02-03), and balance (id*3.33 rounded to 2 decimal places).; `truncations` 4, rows.csv 401 lines, strict check True | 92 s | 17 | 5.4 | 82,562 | 5,910 | ≈1.0k | 4 (truncated×4) | 0 | 0 | append_file 4 / bash 4 / write_file 2 / read_file 1 / run 1 / finish 1 | F5 F5-1024-qwen-r1 |
| `create-fixturesrowscsv-with-a-header-0825224928-5019b19b` (**F5 F5-1024-qwen-r2**, branch runtime, docker) | FAIL | `model_error` — aborted after 3 consecutive empty_reply failures; `truncations` 3, rows.csv 0 lines, strict check None | 61 s | 4 | 15.3 | 10,423 | 3,091 | ≈0.2k | 3 (truncated×3) | 0 | 0 | read_file 1 | F5 F5-1024-qwen-r2 |
| `create-fixturesrowscsv-with-a-header-0825225030-0897cd4a` (**F5 F5-2048-qwen-r1**, branch runtime, docker) | FAIL | `model_error` — aborted after 3 consecutive empty_reply failures; `truncations` 3, rows.csv 0 lines, strict check None | 76 s | 4 | 19.0 | 10,465 | 6,163 | ≈0.2k | 3 (truncated×3) | 0 | 0 | read_file 1 | F5 F5-2048-qwen-r1 |
| `create-fixturesrowscsv-with-a-header-0825225147-fe4b69a5` (**F5 F5-2048-qwen-r2**, branch runtime, docker) | FAIL | `model_error` — aborted after 3 consecutive empty_reply failures; `truncations` 3, rows.csv 0 lines, strict check None | 76 s | 4 | 18.9 | 10,386 | 6,163 | ≈0.2k | 3 (truncated×3) | 0 | 0 | read_file 1 | F5 F5-2048-qwen-r2 |
| `create-fixturesrowscsv-with-a-header-0825225304-3bca6cb6` (**F5 F5-4096-qwen-r1**, branch runtime, docker) | FAIL | `completed` — Created fixtures/rows.csv with header row and 400 data rows following README.md. The file contains:
- Header: id,name,email,plan,created_at,balance
- 400 data rows with id (1-400), name, email, plan cycling by id%3, dates starting 2024-01-01, balance = id*3.33
- 401 total lines (header + 400 rows), ends with exactly one newline, UTF-8, LF line endings.; `truncations` 2, rows.csv 400 lines, strict check False | 1134 s | 27 | 42.0 | 715,150 | 33,731 | ≈0.7k | 2 (truncated×2) | 0 | 0 | bash 15 / append_file 5 / read_file 2 / write_file 2 / finish 1 | F5 F5-4096-qwen-r1 |
| `create-fixturesrowscsv-with-a-header-0825225437-4bf56907` (**F5 F5-1024-dev-r1**, branch runtime, docker) | FAIL | `timeout`; `truncations` 0, rows.csv 401 lines, strict check None | 1801 s | 32 | 56.3 | 1,420,348 | 23,932 | ≈0.8k | 0 (—) | 0 | 0 | append_file 400 / bash 5 / list_dir 2 / read_file 1 / write_file 1 / exit code: 0
id,name,email,plan,created_at,balance
1,User1,user1@example.com,free,2024-01-01,3.33
2,User2,user2@example.com,pro,2024-01-02,6.66
3,User3,user3@example.com,enterprise,2024-01-03,9.99[TOOL_CALLS][TOOL_CALLS]bash 1 | F5 F5-1024-dev-r1 |
| `create-fixturesrowscsv-with-a-header-0825231159-98eabf4f` (**F5 F5-4096-qwen-r2**, branch runtime, docker) | recovery | `completed`; `truncations` 3, rows.csv 401 lines, strict check True — wall shared the GPU with driver B until 23:25 and the live suite after | 937 s | 17 | 55.1 | 250,684 | 27,072 | ≈0.3k | 3 (truncated×3) | 0 | 0 | bash 8 / append_file 3 / read_file 1 / write_file 1 / finish 1 | F5 4096 r2 |
| `create-fixturesrowscsv-with-a-header-0825232737-e3ab27c1` (driver A's row 7 start, killed 5 s in) | — | orphaned run dir, no `run_end`; excluded from every count | — | — | — | — | — | — | — | — | — | — | F5 orphan |
| **F5 score, first pass (qwen, spec §8 rule)** | **1024 FAIL (recovery + strike abort), 2048 FAIL (two strike aborts), 4096 FAIL (400-row completion + recovery)** | the finding: with LM Studio a cut tool call is dropped, so a cut-off reply is a *text-only* reply and the runner records a `FailureTracker` `empty_reply` strike **before** the six-cut-off check (`runner.py` ~1032) — three consecutive cut-offs abort at turn 4 on the old rule and the spec's six-budget is unreachable for them. The two recoveries (1024 r1: 4 cut-offs/17 turns/92 s; 4096 r2: 3 cut-offs/17 turns) show the cap-aware target working once the model gets past its first attempts. **Owner decision (23:47): cut-off replies count only toward `MAX_TRUNCATED_REPLIES`; the three-strike rules stay for genuinely empty replies and non-length malformed calls** → task W5 under released 0.10.1, then the F5 rows rerun serially | — | — | — | — | — | — | — | — | — | — | F5 verdict |
| `issue-65-task-w5-owner-0825232940-e8deaaca` (**W5**, fresh run, released 0.10.1) | runner exactly per brief; one test off | `max_turns` — the three runner sites and the text clause landed in 10 edits; four new/renamed tests; `tests/test_runner.py` 198 passed / 1 failed (the worker added `max_tokens=1024` to a Runner whose expected text the helper builds at the default cap, and swapped the `bad arguments` pin for a vacuous one); no `finish`, no full suite in 60 turns → feedback resume 1 | 178 s | 60 | 3.0 | 1,600,979 | 10,070 | ≈9.0k | 0 (—) | 0 | 0 | read_file 27 / bash 12 / grep 11 / edit_file 10 | W5 |
| `issue-65-task-w5-owner-0825233333-3dda65d8` (**W5 feedback resume 1**, released 0.10.1) | **applied the feedback** — the contrast case | `completed` — removed the stray `max_tokens=1024`, restored the `bad arguments` pin, ran `tests/test_runner.py` (199) and the full suite before `finish`; 33 `bash` calls, most of them pytest. Verified on the worktree: **1549 passed, 1 skipped**. Merged as one worker-verbatim commit, no Claude finish | 277 s | 37 | 7.5 | 584,174 | 3,634 | ≈2.1k | 0 (—) | 0 | 0 | bash 33 / edit_file 2 / read_file 1 / finish 1 | W5 resume 1 |
| (**C5 unit suite, final code HEAD** 00ea23d = W5 merged) | **1549 passed / 1 skipped / 38 deselected** (110 s) | 1505 at the start of the build → 1549 | — | — | — | — | — | — | — | — | — | — | C5 unit |
| **F5 reruns, serial pass** (after W5; one driver, nothing else on the GPU; started 23:40) | qwen: **1024 PASS**, 2048 tie-break owed, **4096 PASS** | the six-cut-off budget is now reached: every abort below is `aborted after 6 cut-off replies at --max-tokens <cap>` (bench `abort_kind` = `truncated`), none is the three-strike abort | | | | | | | | | | | | F5 serial |
| `create-fixturesrowscsv-with-a-header-0825234041-29d22b34` (**F5 F5-1024-qwen-r1**, serial, branch runtime at 00ea23d, docker) | trunc-abort (allowed at 1024) | `model_error` — aborted after 6 cut-off replies at --max-tokens 1024: raise --max-toke; `truncations` 6, rows.csv 0 lines, strict check None — six consecutive cut-offs, the budget ends it at turn 7 — the model never split its write at 1024 | 79 s | 7 | 11.3 | 19,775 | 6,160 | ≈0.3k | 6 (truncated×6) | 0 | 0 | read_file 1 | F5 F5-1024-qwen-r1 |
| `create-fixturesrowscsv-with-a-header-0825234202-b2fc1f59` (**F5 F5-1024-qwen-r2**, serial, branch runtime at 00ea23d, docker) | trunc-abort (allowed at 1024) | `model_error` — aborted after 6 cut-off replies at --max-tokens 1024: raise --max-toke; `truncations` 6, rows.csv 0 lines, strict check None — same shape: six consecutive cut-offs, turn 7 | 82 s | 7 | 11.7 | 20,682 | 6,187 | ≈0.3k | 6 (truncated×6) | 0 | 0 | read_file 1 | F5 F5-1024-qwen-r2 |
| `create-fixturesrowscsv-with-a-header-0825234324-50509f75` (**F5 F5-2048-qwen-r1**, serial, branch runtime at 00ea23d, docker) | **recovery** | `completed`; `truncations` 4, rows.csv 401 lines, strict check True — 4 cut-offs, then write_file + append_file in chunks; strict check True | 162 s | 17 | 9.5 | 146,635 | 12,902 | ≈1.0k | 4 (truncated×4) | 0 | 0 | bash 8 / read_file 2 / write_file 1 / append_file 1 / finish 1 | F5 F5-2048-qwen-r1 |
| `create-fixturesrowscsv-with-a-header-0825234608-639218be` (**F5 F5-2048-qwen-r2**, serial, branch runtime at 00ea23d, docker) | six-cut-off abort → tie-break owed | `model_error` — aborted after 6 cut-off replies at --max-tokens 2048: raise --max-toke; `truncations` 6, rows.csv 0 lines, strict check None — six consecutive cut-offs at 2048; the pair disagrees → one tie-break rerun (F5-2048-qwen-r3) after the plan | 146 s | 7 | 20.9 | 19,864 | 12,304 | ≈0.2k | 6 (truncated×6) | 0 | 0 | read_file 1 | F5 F5-2048-qwen-r2 |
| `create-fixturesrowscsv-with-a-header-0825234835-180c6983` (**F5 F5-4096-qwen-r1**, serial, branch runtime at 00ea23d, docker) | clean completion (cap never bound) | `completed`; `truncations` 0, rows.csv 401 lines, strict check True — no cut-off at all; 11 bash calls + one write_file; strict check True — not a "recovery" by the letter, scored as a pass and said so | 40 s | 15 | 2.6 | 144,616 | 2,178 | ≈3.7k | 0 (—) | 0 | 0 | bash 11 / read_file 2 / write_file 1 / finish 1 | F5 F5-4096-qwen-r1 |
| `create-fixturesrowscsv-with-a-header-0825234915-a4aac7e0` (**F5 F5-4096-qwen-r2**, serial, branch runtime at 00ea23d, docker) | **recovery** | `completed`; `truncations` 1, rows.csv 401 lines, strict check True — 1 cut-off, then write_file + 3 append_file; strict check True | 223 s | 9 | 24.8 | 103,834 | 18,348 | ≈0.5k | 1 (truncated×1) | 0 | 0 | append_file 3 / bash 2 / read_file 1 / write_file 1 / finish 1 | F5 F5-4096-qwen-r2 |
| `create-fixturesrowscsv-with-a-header-0825235300-1bdb63ef` (**F5 F5-1024-dev-r1**, serial, branch runtime at 00ea23d, docker) | six-cut-off abort at turn 17 (rule: ≤16) | `model_error` — aborted after 6 cut-off replies at --max-tokens 1024: raise ; `truncations` 6, strict check None — six cut-offs, the budget ends it one turn past the rule's limit; csv 1 line | 286 s | 17 | 16.8 | 158,320 | 7,464 | ≈0.6k | 6 (truncated×6) | 0 | 0 | read_file 4 / bash 4 / list_dir 2 / write_file 1 | F5 F5-1024-dev-r1 |
| `create-fixturesrowscsv-with-a-header-0825235747-29ecbf21` (**F5 F5-1024-dev-r2**, serial, branch runtime at 00ea23d, docker) | inconclusive (#67 `unknown_tool`) | `model_error` — aborted after 3 consecutive unknown_tool failures; `truncations` 0, strict check None — wrote all 401 rows, then three consecutive `[TOOL_CALLS]`-mangled tool names → `unknown_tool` abort at turn 30; 1237 s | 1237 s | 30 | 41.2 | 1,260,541 | 24,331 | ≈1.0k | 0 (—) | 0 | 0 | append_file 400 / list_dir 2 / bash 2 / read_file 1 / write_file 1 | F5 F5-1024-dev-r2 |
| `create-fixturesrowscsv-with-a-header-0826001824-e6d1e8f9` (**F5 F5-2048-dev-r1**, serial, branch runtime at 00ea23d, docker) | inconclusive (#67) | `model_error` — aborted after 3 consecutive unknown_tool failures; `truncations` 0, strict check None — `unknown_tool` ×3 at turn 9 | 70 s | 9 | 7.8 | 59,355 | 1,667 | ≈0.9k | 0 (—) | 0 | 0 | bash 4 / list_dir 2 / read_file 2 / write_file 1 / exit code: 0
4 1 | F5 F5-2048-dev-r1 |
| `create-fixturesrowscsv-with-a-header-0826001936-3e008eb2` (**F5 F5-2048-dev-r2**, serial, branch runtime at 00ea23d, docker) | inconclusive (#67) — second → pair FAIL, attributed to #67 | `model_error` — aborted after 3 consecutive unknown_tool failures; `truncations` 5, strict check None — 5 cut-offs handled, 401 rows written, then `unknown_tool` ×3 at turn 24 | 392 s | 24 | 16.3 | 214,264 | 12,171 | ≈0.6k | 5 (truncated×5) | 0 | 0 | bash 13 / read_file 3 / exit code: 0
1 3 / list_dir 2 / write_file 1 | F5 F5-2048-dev-r2 |
| `create-fixturesrowscsv-with-a-header-0826002608-5e9b13ed` (**F5 F5-4096-dev-r1**, serial, branch runtime at 00ea23d, docker) | inconclusive (#67) | `model_error` — aborted after 3 consecutive unknown_tool failures; `truncations` 0, strict check None — `unknown_tool` ×3 at turn 18 | 110 s | 18 | 6.1 | 140,543 | 3,137 | ≈1.3k | 0 (—) | 0 | 0 | bash 12 / read_file 3 / list_dir 2 / write_file 1 /      1	id,name 1 | F5 F5-4096-dev-r1 |
| `create-fixturesrowscsv-with-a-header-0826002759-f5c1a1cd` (**F5 F5-4096-dev-r2**, serial, branch runtime at 00ea23d, docker) | completed, content wrong (strict check False) | `completed`; `truncations` 0, strict check False — no cut-off; 96 `append_file` calls + 22 bash; tool names in the transcript carry `[TOOL_CALLS]` residue (#67); two `no_change` nudges during the verification turns | 767 s | 31 | 24.7 | 1,129,109 | 19,319 | ≈1.5k | 2 (no_change×2) | 0 | 0 | append_file 96 / bash 22 / list_dir 1 / read_file 1 / write_file 1 | F5 F5-4096-dev-r2 |
| **F5 serial pass, Devstral** | **1024: tie-break running; 2048: FAIL attributed to #67 (two inconclusive); 4096: tie-break** | five of six runs end on #67's `[TOOL_CALLS]` shape (`unknown_tool` ×3) — the spec's F5 rule names #67 for exactly this; the six-cut-off budget bounded the one truncation-heavy run (1024 r1) and the cap-aware chunks let 2048 r2 write all 401 rows through five cut-offs before #67 took it | — | — | — | — | — | — | — | — | — | — | F5 verdict (dev) |
| (**observation**, dev-4096-r2 `f5c1a1cd`) | the plain-mode `no_change` nudge, live | last edit at turn 10 (`append_file`), then bash-only verification; `nudge{no_change}` at turns 20 and 30 (`NO_CHANGE_RECENT`: "if the task is complete, call finish"), `finish` at turn 31 — the K=10 nudge that C1 (d)/(d2) never reached because qwen batches its reads, observed on a real run | — | — | — | — | — | — | — | — | — | — | guard obs |
| `create-fixturesrowscsv-with-a-header-0826004121-30d797ca` (**F5 F5-2048-qwen-r3 tie-break**, serial, branch runtime at 00ea23d, docker) | **recovery** → qwen 2048 PASS on majority (recovery, six-abort, recovery) | `completed`; `truncations` 3, rows.csv 401 lines, strict check True — 3 cut-offs, then chunked writes; strict check True | 267 s | 14 | 19.1 | 166,307 | 20,459 | ≈0.7k | 3 (truncated×3) | 0 | 0 | append_file 7 / read_file 1 / write_file 1 / bash 1 / finish 1 | F5 F5-2048-qwen-r3 |
| `create-fixturesrowscsv-with-a-header-0826004549-d2632c8e` (**F5 F5-1024-dev-r3 tie-break**, serial, branch runtime at 00ea23d, docker) | clean completion (no cut-off) → 1024-dev split: six-abort at turn 17 / #67 abort / clean | `completed`; `truncations` 0, rows.csv 401 lines, strict check True — Devstral wrote in small chunks from the start; strict check True | 61 s | 14 | 4.4 | 44,918 | 1,817 | ≈0.8k | 0 (—) | 0 | 0 | bash 9 / list_dir 2 / read_file 1 / write_file 1 / exit code: 0
0 1 | F5 F5-1024-dev-r3 |
| `create-fixturesrowscsv-with-a-header-0826004718-e37fa602` (**F5 F5-4096-dev-r3 tie-break**, serial, branch runtime at 00ea23d, docker) | recovery by the letter; strict check False | `completed`; `truncations` 1, rows.csv 401 lines, strict check False — 20 minutes for one Devstral completion whose content the strict check rejects; #67 residue in the transcript | 1203 s | 15 | 80.2 | 617,974 | 26,630 | ≈0.5k | 1 (truncated×1) | 0 | 0 | append_file 400 / bash 8 / list_dir 2 / read_file 1 / write_file 1 | F5 4096-dev-r3 |
| **F5 verdict (spec §8), serial pass + tie-breaks, branch at 00ea23d** | **qwen: 1024 PASS, 2048 PASS (majority), 4096 PASS. Devstral: 1024 split, 2048 FAIL (#67), 4096 PASS by the letter** | qwen — 1024: two six-cut-off aborts at turn 7 (~80 s; the allowed early abort — the model never split at 1024, the budget bounded it); 2048: recovery / six-abort / recovery (17, 7, 14 turns; the two recoveries pass the strict check); 4096: a clean 40 s completion the cap never bound + a recovery (9 turns), both strict-pass. Devstral — 1024: six-abort at turn 17 (one over the rule's 16) / #67 `unknown_tool` / clean 62 s completion (strict pass); 2048: two #67 aborts = FAIL attributed to #67 by the rule's own clause; 4096: #67 abort / clean / recovery, both completions strict-fail, 13–20 min each. Against the first pass (three turn-4 strike aborts): the strike no longer pre-empts the budget, every abort is the six-cut-off text, and the 40-turn / 900 s sink never appeared (longest qwen run 268 s). Devstral's side is #67's | — | — | — | — | — | — | — | — | — | — | F5 verdict |

## Cross-build scoreboard (released-dirtywork builds, per Jim's ask on 2026-08-25)

Computed from this ledger's rows by a script (`scoreboard.py`, session scratch; worker-task rows vs C1/F5 acceptance rows by label), task counts by hand from the plans; the *Claude finishes* column counts Claude-authored rows (finishes and fixes; for #61 it also includes the live-suite rows I ran), so treat it as ±2. Wall and s/turn are on one box with a shared GPU; #65/#66's acceptance minutes include 12 F5 rows run under the confounded parallel arrangement (see the F5 rows). tok/turn ≈ prompt tokens per turn.

| build (tasks) | worker runs | turns | wall | prompt tok | tok/turn | s/turn | completed | max_turns | resumes | zero-change resumes | Claude finishes | acceptance runs (turns, wall) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| #60 (reruns only; no worker rows in this ledger) (—) | 4 | 58 | 9 min | 0.0 M | 0 | 9.4 | 0 | 0 | 0 | 0 | 0 | 2 (11, 5 min) |
| #64 (2026-08-24) (1) | 6 | 188 | 21 min | 4.2 M | 22,526 | 6.8 | 2 | 4 | 5 | 0 | 0 | 0 (0, 0 min) |
| #61 (2026-08-25) (~10) | 35 | 1429 | 130 min | 40.1 M | 28,029 | 5.5 | 16 | 18 | 18 | 4 | 9 | 0 (0, 0 min) |
| #65/#66 (2026-08-25/26) (15) | 27 | 1142 | 113 min | 31.4 M | 27,513 | 5.9 | 9 | 17 | 14 | 2 | 8 | 33 (375, 142 min) |
| #67 (2026-08-26) (7) | 12 | 449 | 45 min | 11.1 M | 24,655 | 6.0 | 9 | 3 | 4 | **1 refused (`unchanged`)**, 3 applied | 1 | 7 (acceptance: 6 + 1 rerun; ~125 min) |

Readings: prompt volume per turn is flat across builds (22–28 k) — the reads-heavy loop is the cost, not the harness; the worker's completion rate on first runs is ~⅓ in every build and a Claude finish follows most feedback rounds; zero-change feedback resumes under the released runtime: 0 (#64) → 4 (#61) → 2 (#65/#66, both before W3 landed the guard on the branch). The next build runs on a runtime that refuses them — the column to watch.
| (**C5 live suite, final code HEAD** 00ea23d, `dirtywork-worker-pytest:0.10`, serial after F5) | **26 passed / 3 skipped / 0 failed** (367 s) | the three skips are the .NET-image conditions (#70) | — | — | — | — | — | — | — | — | — | — | C5 live |
| (**correction**, 2026-08-26, from CI on the 0.11.0 release PR #77) | test 24 / #75 scope | the seed's flattening of nested `.git` directories is **host-tar-specific**: macOS bsdtar drops them, the Linux runner's GNU tar keeps them, so test 24 as merged in #76 failed CI on main (`136b69d`) at its "seed flattened them" assertion. Fixed in #77: the test removes and recreates the nested repositories in the container and asserts neither behaviour; #75 narrowed accordingly. The W4b rows above record what was believed at the time | — | — | — | — | — | — | — | — | — | — | correction |

## #67 — marker-polluted tool names: recover the call, nudge the model (2026-08-26)

Built by the released **dirtywork 0.11.1** (`pipx run --spec 'dirtywork==0.11.1'`), qwen3-coder-next on LM Studio (65k context, parallel 4), worker image `dirtywork-worker-pytest:0.11` (the published `:0.11` + `python3-pytest`), `--verify` = the unit suite, `--verify-rounds 2 --max-turns 60 --timeout 1800`. **The first build on a runtime with the change guard:** a feedback resume that changes nothing is refused by the runtime itself — the `unchanged` status / `unchanged_finish` nudge column is this section's headline. Spec: `docs/superpowers/specs/2026-08-26-marker-polluted-tool-names-design.md` (v3); plan: `docs/superpowers/plans/2026-08-26-issue-67-tool-name-recovery.md`. Sampler: `metrics-67.csv`.

| run (slug) | task | status | wall | turns | s/turn | prompt tok | compl tok | tok/s | nudges | guardrail | resets | tool mix | verify |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `issue-67-task-t0-of-0826150848-0d8b0e5a` | **T0** (fresh run, released 0.11.1) | `completed`, `changed: true` — **the guard, live on the released runtime, first task of the build:** ten read-only turns → `nudge{no_change}` at turn 10; the worker's turn-13 text-only completion with nothing changed was **refused** (`nudge{unchanged_finish}` via `user`); it then wrote `tests/markers.py` (byte-exact per the brief) and added the import to `tests/test_runner.py` — but left line 349's literal tag and never touched `tests/test_soak_tools.py` (steps 2½, 3, 4 undone) → feedback resume 1 | 89 s | 18 | 4.9 | 104,132 | 1,578 | ≈1.2k | 2 (no_change/unchanged_finish) | 0 | 0 | read_file 11 / list_dir 3 / grep 2 / write_file 1 / edit_file 1 | tests/test_runner.py 199 passed on the worktree |
| `issue-67-task-t0-of-0826151113-ac27c1d6` | **T0 feedback resume 1** (released 0.11.1) | **`unchanged`** — **the headline row: the first zero-change feedback resume on a released runtime that refuses it.** Two `bash` calls and one `read_file`, no edit; the turn-4 completion refused (`nudge{unchanged_finish}` via `user`), the second zero-change completion ended the run `unchanged` (exit 1) in 12.6 s — the shape 0.10.1 recorded as `completed` six times in the #61 build and twice in #65/#66. → Claude finish (three one-line edits) | 13 s | 5 | 2.5 | 29,406 | 353 | ≈2.4k | 1 (unchanged_finish) | 0 | 0 | bash 2 / read_file 1 | — |
| — (**T0 Claude finish**, after the resume ended `unchanged`) | landed `7e4a235` + `65f986e` | three one-line edits (`test_runner.py:349` → the helper; `test_soak_tools.py` comment, line 471 and the relative import). Attribution note: `git stash` does not carry an untracked file, so the verbatim commit holds `tests/markers.py` alone and the worker's one import line rode in the finish commit. Greps: zero literal tags outside `tests/markers.py`. Suite on the worktree **1549 passed / 1 skipped**; `test_runner`+`test_soak_tools` 272 on the merged tree | — | — | — | — | — | — | — | — | — | — | T0 finish |
| `issue-67-task-t2a-of-0826152422-ace3cdcb` | **T2a run 1** (released 0.11.1) — **discarded** | `max_turns` — two faults, one mine: (1) the run branched from `3234859` (T0's finish), **without T1**, because the T1 fast-forward had failed in the main checkout — another session had switched that checkout to `issue-82-operator-contract` at 15:17, so my chain's merge and ledger commit ran on the wrong branch and the deleted T1 branch left its commit only in the object store; (2) the worker's `edit_file` deleted four live lines of the call loop (`raw_args`, `args`, `abort_reason`, `terminal`) and re-implemented `recover_name` wrongly at the end of `toolspec.py` — 119 failures. Four `no_change` nudges (forty read/grep turns). Recovery: T1 restored from `0ab5447`, the stray ledger row cherry-picked, #67 moved into its own worktree; T2a relaunched | 118 s | 60 | 2.0 | 1,426,878 | 4,619 | ≈12.1k | 4 (no_change/no_change/no_change/no_change) | 0 | 0 | read_file 32 / grep 21 / edit_file 5 / list_dir 1 / bash 1 | 119 failed |
| — (**process**) | the shared checkout | `#65/#66` ran from a dedicated integration worktree; #67 began on the main checkout's branch, which another session switched under it. Rule restated: every build gets its own `.worktrees/<branch>` and every git operation of the build happens there; the launcher's `--branch-from` names the branch, so it was never the problem | — | — | — | — | — | — | — | — | — | — | process |
| `issue-67-task-t1-of-0826151706-8bdf875a` | **T1** (fresh run, released 0.11.1) | `completed`, `changed: true` — every brief item landed: `TOOL_CALL_MARKERS` by concatenation with the comment copied, `recover_name` verbatim to the spec, the 40+…+40 error echo, the runner import after `.tools`, tests a–j (10). No feedback round, no Claude finish. Nit left as is: the new imports sit mid-file in `test_toolspec.py`. Suite on the worktree **1559 passed / 1 skipped** | 271 s | 52 | 5.2 | 1,472,699 | 6,497 | ≈5.5k | 0 (—) | 0 | 0 | read_file 23 / bash 16 / edit_file 11 / grep 3 / glob 1 / list_dir 1 / apply_edits 1 / finish 1 | 1559 passed |
| `issue-67-task-t2a-of-0826153110-ca81b9ef` | **T2a run 2** (fresh run, released 0.11.1, base with T1) | `completed`, `changed: true` — every brief item: recovery before the `tc.error` check, `cap_name`/`TOOL_NAME_TRANSCRIPT_CHARS`, the assistant-event name cap, `transcript_name` for `tool_result.tool` and `last_tool_result.tool`, sparse `tool_raw`, tests a–f. No feedback round. Nit for the style pass: trailing whitespace on two blank lines in the loop. Suite on the worktree **1565 passed / 1 skipped** | 278 s | 48 | 5.8 | 894,080 | 8,844 | ≈3.2k | 1 (no_change) | 0 | 0 | read_file 14 / bash 14 / edit_file 10 / grep 7 / glob 1 / list_dir 1 / finish 1 | 1565 passed |
| `issue-67-task-t2b-of-0826153833-63516e09` | **T2b run 1** (fresh run, released 0.11.1) | `max_turns` — the runner half is exactly the brief (`NAME_RECOVERED_NUDGE`, composition beside `timeout_text`, both delivery points, order `timeout → name_recovered → stall`); seven tests written, two fail because their scripts end the run on the nudged turn (finish in the same response as the timed-out call; a polluted `finish` as the third turn) — the runner correctly writes no nudge on an ending turn. No `finish` in 60 turns → feedback resume 1 (two script fixes) | 243 s | 60 | 4.0 | 1,431,451 | 12,860 | ≈5.9k | 2 (no_change/no_change) | 0 | 0 | read_file 25 / grep 14 / edit_file 11 / bash 10 | 2 failed / 1570 passed |
| `issue-67-task-t2b-of-0826154535-5460b7f4` | **T2b feedback resume 1** (released 0.11.1) | `completed` — applied the feedback: both scripts fixed (a separate `done` response after the timed-out call; three polluted `bash` turns then a clean `finish`), ran the file and the suite, `finish`. `test_runner.py` 213 passed; full suite on the worktree green. Merged as one worker-verbatim commit, no Claude finish | 170 s | 17 | 10.0 | 202,821 | 3,229 | ≈1.2k | 0 (—) | 0 | 0 | bash 6 / read_file 6 / edit_file 3 / grep 1 / finish 1 | green |
| `issue-67-task-t3-of-0826155037-47c509ef` | **T3 run 1** (fresh run, released 0.11.1) | `completed`, `changed: true` — bench `NUDGE_KINDS` 11 + the eleven-wide exact-cell pin; harvest S6 (three triggers, `TOOL_CALL_MARKERS` imported), `recovered` at both sites, docstring S6/S14; tests. Suite on the worktree **1574 passed**. One test breaks two rules: the Devstral token spelled literally twice (T0 had just removed the last one) and a fixed `/tmp` path instead of `tmp_path` → feedback resume 1 | 304 s | 44 | 6.9 | 1,329,851 | 10,010 | ≈4.4k | 0 (—) | 0 | 0 | read_file 18 / edit_file 14 / bash 14 / grep 2 / finish 1 | 1574 passed |
| `issue-67-task-t3-of-0826155832-762db726` | **T3 feedback resume 1** (released 0.11.1) | `completed` — applied the feedback: the two literal markers → `TOOL_CALLS`, the fixed `/tmp` path → `tmp_path`, trailing whitespace stripped; `test_soak_tools.py` 75 passed; suite green on the worktree. Merged as one worker-verbatim commit | 138 s | 12 | 11.5 | 116,883 | 1,668 | ≈0.9k | 0 (—) | 0 | 0 | read_file 4 / grep 3 / edit_file 2 / bash 2 / finish 1 | green |
| `issue-67-task-d1-of-0826160257-3dbd8dce` | **D1 run 1** (fresh run, released 0.11.1, qwen) | `max_turns`, `changed: true` — every documented row landed (schema version sentence, `tool_calls` name cap, open `tool` row, `tool_raw` row, `follow_up` order + verify clause, merge-order sentence, `kind` row, both `last_tool_result` rows; contract string + eleven kinds + the #67 sentence; operating entry; schema-test kind list + a polluted call in `_NudgingProvider`). The Devstral token in doc prose survived qwen's tool channel intact. Two prose warts: my instruction sentence pasted into the merge-order paragraph; the cap phrase inside the `nudge` parenthetical → feedback resume 1. Suite: one flaky failure in the first run (not reproduced on two reruns, name not captured), **1574 passed** | 376 s | 60 | 6.3 | 2,428,585 | 8,869 | ≈6.5k | 1 (no_change) | 0 | 0 | read_file 33 / edit_file 15 / bash 12 / grep 5 / insert_after 1 | 1574 passed |
| `issue-67-task-d1-of-0826162050-9855930d` | **D1 feedback resume 1** (released 0.11.1) | `completed` — applied both prose fixes (the pasted instruction removed; the cap phrase moved to `tool_result (truncated; …)`); schema test 12 passed; suite green. Merged as one worker-verbatim commit. **Worker tasks complete: 6 tasks, 8 fresh runs (one discarded), 5 feedback resumes — 1 refused as `unchanged`, 4 applied — 1 Claude finish (three lines)** | 96 s | 13 | 7.4 | 179,782 | 1,768 | ≈1.9k | 0 (—) | 0 | 0 | read_file 5 / edit_file 4 / bash 3 / finish 1 | green |
| `create-fixturesrowscsv-with-a-header-0826162439-44b73fc1` | **C1 F5-1024-dev-r1** (branch 43f5148, serial) | **PASS** (no `unknown_tool` abort) | `completed`; recovered 2, unknown-tool errors 0, strict check False | 1318 s | 37 | 35.6 | 1,840,441 | 25,449 | ≈1.4k | 2 (name_recovered×2) | 0 | 0 | append_file 400 / bash 10 / list_dir 2 / read_file 1 | C1 F5-1024-dev-r1 |
| `create-fixturesrowscsv-with-a-header-0826164639-aeb9ac93` | **C1 F5-1024-dev-r2** (branch 43f5148, serial) | **PASS** (no `unknown_tool` abort) | `completed`; recovered 1, unknown-tool errors 0, strict check False | 1192 s | 38 | 31.4 | 2,031,856 | 24,245 | ≈1.7k | 1 (name_recovered×1) | 0 | 0 | append_file 400 / bash 12 / list_dir 1 / read_file 1 | C1 F5-1024-dev-r2 |
| `create-fixturesrowscsv-with-a-header-0826170632-b231997d` | **C1 F5-2048-dev-r1** (branch 43f5148, serial) | **FAIL** — `unknown_tool` ×3 on the *sanitised* shape (§0.3) | `model_error`; recovered 4, unknown-tool errors 3, strict check None | 269 s | 14 | 19.2 | 174,630 | 7,530 | ≈0.7k | 3 (truncated×2/name_recovered×1) | 0 | 0 | bash 16 / read_file 2 / list_dir 1 / write_file 1 | C1 F5-2048-dev-r1 |
| — (**C1 finding**) | the sanitised shape | 2048-dev-r1 recovered four raw-marker calls and then aborted on three names of the form `exit_code_0_17285_fixtures_rows_csv_TOOL_CALLS_bash` — the pollution after LM Studio's tool-name sanitiser (`[A-Za-z0-9_-]`) rewrote it. Spec §0.3 folds the sanitised twin of every marker into `TOOL_CALL_MARKERS` (one derivation rule); task T4; the failed rows rerun after | — | — | — | — | — | — | — | — | — | — | C1 finding |
| `issue-67-task-t4-added-0826171311-7c9fa68f` | **T4** (fresh run, released 0.11.1; GPU shared with the C1 driver) | `completed`, `changed: true` — exactly the brief: `_RAW_MARKERS` + the derived sanitised twins (ten markers), four `recover_name` cases, the harvest's fourth S6 case; a text completion at turn 60. No feedback round. Suite on the worktree **1578 passed** | 617 s | 60 | 10.3 | 1,453,388 | 9,076 | ≈2.4k | 1 (no_change) | 0 | 0 | read_file 26 / bash 22 / edit_file 8 / grep 3 / list_dir 2 | 1578 passed |
| `create-fixturesrowscsv-with-a-header-0826171103-270fd891` | **C1 F5-2048-dev-r2** (branch 43f5148, GPU shared with T4) | **PASS** (no `unknown_tool` abort) | `completed`; recovered 4, unknown-tool errors 0, strict check False | 831 s | 32 | 26.0 | 674,561 | 21,548 | ≈0.8k | 4 (name_recovered×3/no_change×1) | 0 | 0 | append_file 41 / bash 14 / list_dir 2 / read_file 1 | C1 F5-2048-dev-r2 |
| (**C2 unit suite, final code HEAD** 8c41b8a = T4 + style pass) | **1578 passed / 1 skipped / 38 deselected** (112 s) | 1549 at the start of the build → 1578 | — | — | — | — | — | — | — | — | — | — | C2 unit |
| (**C2 live suite, final code HEAD** 8c41b8a, `dirtywork-worker-pytest:0.11`, all files) | **29 passed / 0 failed / 0 skipped** (399 s) | the three .NET tests the `:0.10`-based image skipped now run on `:0.11` and pass — the #70 W^X fix verified live as a side effect | — | — | — | — | — | — | — | — | — | — | C2 live |
| `create-fixturesrowscsv-with-a-header-0826172455-3330f634` | **C1 F5-4096-dev-r1** (branch 43f5148; GPU shared part of the time) | **PASS** (no `unknown_tool` abort) | `completed`; recovered 0, unknown-tool errors 0, strict check False | 1186 s | 12 | 98.8 | 442,053 | 26,397 | ≈0.4k | 1 (truncated×1) | 0 | 0 | append_file 400 / bash 4 / list_dir 1 / read_file 1 | C1 F5-4096-dev-r1 |
| `create-fixturesrowscsv-with-a-header-0826174442-f671754d` | **C1 F5-4096-dev-r2** (branch 43f5148; GPU shared part of the time) | **PASS** (no `unknown_tool` abort) | `completed`; recovered 14, unknown-tool errors 0, strict check True | 252 s | 21 | 12.0 | 276,144 | 7,205 | ≈1.1k | 7 (truncated×1/name_recovered×5/no_change×1) | 0 | 0 | bash 29 / list_dir 2 / read_file 1 / write_file 1 | C1 F5-4096-dev-r2 |
| `create-fixturesrowscsv-with-a-header-0826174944-40a3dd16` | **C1 F5-2048-dev-r1-rerun** (final code b19fea4 = 8c41b8a + ledger, alone on the GPU) | **PASS** (no `unknown_tool` abort) | `stuck` — the pre-existing repeat guard (the same failing `bash` command four times), not #67's; recovered 8, unknown-tool errors 0; the sanitised shape did not recur in this run | 755 s | 28 | 27.0 | 817,643 | 20,447 | ≈1.1k | 4 (name_recovered×4) | 0 | 0 | append_file 73 / bash 21 / list_dir 2 / read_file 1 | C1 rerun |
| **C1 verdict (#67, spec §6)** | **PASS — 6/6 rows without an `unknown_tool` abort** (5 on `43f5148`, the sixth rerun on the final code) | across the seven acceptance runs: 30 polluted calls recovered (`tool_raw`), 12 `name_recovered` nudges, 0 unknown-tool errors, 0 `unknown_tool` aborts — against last night's 5 aborts in 8 Devstral runs. The first pass surfaced the sanitised shape (§0.3, T4), whose fix is pinned by tests and not yet observed live. Devstral's strict content checks remain false, as before | — | — | — | — | — | — | — | — | — | — | C1 verdict |
| (**sampler**, `metrics-67.csv`) | 2 228 samples, 14:51–18:03 CDT | free RAM min 0.01 GB, median 6.0 GB, 225 samples under 1 GB — the periods with a worker run and the C1 driver sharing the GPU (T4 + rows 3–5); Devstral and qwen both resident | — | — | — | — | — | — | — | — | — | — | metrics |

## #82 — operator contract in the wheel (plan v1.1)

Baseline 2026-08-26 20:30 CDT on `issue-82-operator-contract` (rebased onto main @7e6f775): `/usr/bin/python3 -m pytest -q -p no:cacheprovider` → 1579 passed, 1 skipped, 38 deselected in 91.14s (0:01:31). Runtime: released dirtywork 0.11.1 via `pipx run`, worker `qwen/qwen3-coder-next` (LM Studio, 65,536 ctx), image `dirtywork-worker-pytest:0.11`, sampler `metrics-82.csv` from 20:29.

| Task | Slug | Status | Turns | Wall | s/turn | Prompt tok | Compl tok | tok/s | Nudges | Guardrail | Tool mix | Verify | Resumes | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| W1 | `issue-82-operator-contract-in-0826203153-3e27a2e0` | completed | 25 | 210.7s | 8.4 | 280,950 | 3,896 | ~18.5 compl | 1 | - | 25 tool results (see transcript) | pass (round 1, exit 0) | 0 | first try; all 7 plan checks exact; host suite 1586 passed (1579+7); ff-merged 224f6bb |
